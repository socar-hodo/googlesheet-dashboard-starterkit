---
phase: 07-export
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/dashboard/dashboard-content.tsx
  - components/dashboard/dashboard-header.tsx
autonomous: false
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-03

must_haves:
  truths:
    - "대시보드 상단 오른쪽에 CSV 버튼과 Excel 버튼이 표시된다"
    - "CSV 버튼 클릭 시 현재 탭의 필터링된 데이터가 .csv 파일로 다운로드된다"
    - "Excel 버튼 클릭 시 현재 탭의 필터링된 데이터가 .xlsx 파일로 다운로드된다"
    - "daily 탭에서 CSV 다운로드 시 파일명이 daily-YYYY-MM-DD.csv 형식이다"
    - "weekly 탭에서 Excel 다운로드 시 파일명이 weekly-YYYY-MM-DD.xlsx 형식이다"
    - "기간 필터(period)가 적용된 filteredData만 내보내진다 (전체 data가 아님)"
  artifacts:
    - path: "components/dashboard/dashboard-content.tsx"
      provides: "handleExportCsv, handleExportXlsx 핸들러 + DashboardHeader props 전달"
      contains: "handleExportCsv"
    - path: "components/dashboard/dashboard-header.tsx"
      provides: "CSV/Excel 버튼 UI (Download 아이콘, outline 버튼)"
      contains: "onExportCsv"
  key_links:
    - from: "components/dashboard/dashboard-content.tsx"
      to: "lib/export-utils.ts"
      via: "importexportToCsv, exportToXlsx"
      pattern: "from '@/lib/export-utils'"
    - from: "components/dashboard/dashboard-content.tsx"
      to: "components/dashboard/dashboard-header.tsx"
      via: "onExportCsv={handleExportCsv} onExportXlsx={handleExportXlsx} props"
      pattern: "onExportCsv"
    - from: "handleExportCsv"
      to: "filteredData"
      via: "tab === 'daily' ? filteredData.daily : filteredData.weekly"
      pattern: "filteredData"
---

<objective>
export 핸들러 통합 + 버튼 UI 추가 + 브라우저 검증

Purpose: lib/export-utils.ts의 순수 함수를 DashboardContent의 상태와 연결하여 실제 다운로드가 작동하게 하고, DashboardHeader에 CSV/Excel 버튼을 추가한다.
Output: 작동하는 CSV/Excel 다운로드 버튼이 대시보드 헤더에 배치됨
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-export/07-CONTEXT.md
@.planning/phases/07-export/07-RESEARCH.md
@.planning/phases/07-export/07-01-SUMMARY.md
</context>

<interfaces>
<!-- DashboardContent 현재 구조 (components/dashboard/dashboard-content.tsx) -->

```typescript
// 현재 props interface
interface DashboardContentProps {
  data: TeamDashboardData;
  tab: 'daily' | 'weekly';
  initialPeriod?: string;
}

// 현재 DashboardHeader 호출 (수정 전)
<DashboardHeader tab={tab} period={period} onPeriodChange={handlePeriodChange} />

// filteredData — export 대상 (현재 기간 필터링된 데이터)
const filteredData = useMemo<TeamDashboardData>(() => { ... }, [data, tab, period]);
```

<!-- DashboardHeader 현재 props interface (components/dashboard/dashboard-header.tsx) -->

```typescript
// 현재 (수정 전)
interface DashboardHeaderProps {
  tab: 'daily' | 'weekly';
  period: PeriodKey;
  onPeriodChange: (p: PeriodKey) => void;
}

// 수정 후 목표
interface DashboardHeaderProps {
  tab: 'daily' | 'weekly';
  period: PeriodKey;
  onPeriodChange: (p: PeriodKey) => void;
  onExportCsv: () => void;    // 추가
  onExportXlsx: () => void;   // 추가
}
```

<!-- export-utils.ts에서 가져올 함수 시그니처 (07-01-PLAN에서 생성) -->

```typescript
// lib/export-utils.ts
export function exportToCsv(
  records: DailyRecord[] | WeeklyRecord[],
  tab: 'daily' | 'weekly',
): void;

export function exportToXlsx(
  records: DailyRecord[] | WeeklyRecord[],
  tab: 'daily' | 'weekly',
): void;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DashboardContent에 export 핸들러 추가 + DashboardHeader props 확장</name>
  <files>components/dashboard/dashboard-content.tsx, components/dashboard/dashboard-header.tsx</files>
  <action>
**components/dashboard/dashboard-content.tsx 수정:**

1. import 추가: `import { exportToCsv, exportToXlsx } from '@/lib/export-utils';`

2. handleExportCsv 핸들러 추가 (기존 handlePeriodChange 아래에 배치):
```typescript
const handleExportCsv = useCallback(() => {
  const records = tab === 'daily' ? filteredData.daily : filteredData.weekly;
  exportToCsv(records, tab);
}, [filteredData, tab]);

const handleExportXlsx = useCallback(() => {
  const records = tab === 'daily' ? filteredData.daily : filteredData.weekly;
  exportToXlsx(records, tab);
}, [filteredData, tab]);
```

3. DashboardHeader JSX에 props 추가:
```tsx
<DashboardHeader
  tab={tab}
  period={period}
  onPeriodChange={handlePeriodChange}
  onExportCsv={handleExportCsv}
  onExportXlsx={handleExportXlsx}
/>
```

**components/dashboard/dashboard-header.tsx 수정:**

1. import 추가: `import { Download } from 'lucide-react';`, `import { Button } from '@/components/ui/button';`

2. DashboardHeaderProps 확장:
```typescript
interface DashboardHeaderProps {
  tab: 'daily' | 'weekly';
  period: PeriodKey;
  onPeriodChange: (p: PeriodKey) => void;
  onExportCsv: () => void;
  onExportXlsx: () => void;
}
```

3. 함수 시그니처 업데이트: `export function DashboardHeader({ tab, period, onPeriodChange, onExportCsv, onExportXlsx }: DashboardHeaderProps)`

4. JSX 오른쪽 영역에 export 버튼 그룹 추가:
```tsx
{/* 오른쪽: 기간 선택 + 내보내기 버튼 */}
<div className="flex items-center gap-2">
  <PeriodFilter
    periods={tab === 'daily' ? DAILY_PERIODS : WEEKLY_PERIODS}
    active={period}
    onChange={onPeriodChange}
  />
  <div className="h-4 w-px bg-border" />
  <Button variant="outline" size="sm" onClick={onExportCsv}>
    <Download className="h-4 w-4 mr-1" />
    CSV
  </Button>
  <Button variant="outline" size="sm" onClick={onExportXlsx}>
    <Download className="h-4 w-4 mr-1" />
    Excel
  </Button>
</div>
```

현재 `dashboard-header.tsx`의 JSX는 `<PeriodFilter .../>` 단독으로 오른쪽에 있다. 이를 `<div className="flex items-center gap-2">` 래퍼로 감싸고 구분선 + 두 버튼을 추가한다. 기존 outer `flex items-center justify-between` 구조는 유지한다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TypeScript 컴파일 오류 없음
    - DashboardContent에 handleExportCsv, handleExportXlsx useCallback 추가
    - DashboardHeader에 onExportCsv, onExportXlsx props 추가
    - DashboardHeader UI에 CSV, Excel 버튼 (Download 아이콘 포함) 표시
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
CSV/Excel 내보내기 버튼 — DashboardHeader 오른쪽 영역(기간 필터 옆)에 CSV + Excel 버튼 추가, 클릭 시 현재 필터링된 데이터가 다운로드됨
  </what-built>
  <how-to-verify>
1. 개발 서버 실행: `npm run dev` (이미 실행 중이면 생략)
2. http://localhost:3000/dashboard 접속
3. 대시보드 헤더 오른쪽에 "CSV" 버튼과 "Excel" 버튼이 기간 필터 옆에 표시되는지 확인
4. "이번 주" 또는 임의 기간 선택 후 CSV 버튼 클릭 → `daily-2026-MM-DD.csv` 파일이 다운로드되는지 확인
5. 다운로드된 CSV 파일을 열어 헤더(날짜,매출,손익,이용시간,이용건수,가동률)와 데이터가 정상인지, 한국어가 깨지지 않는지 확인
6. Excel 버튼 클릭 → `daily-2026-MM-DD.xlsx` 파일이 다운로드되는지 확인
7. "주차별" 탭으로 전환 후 CSV 클릭 → `weekly-2026-MM-DD.csv`, Excel 클릭 → `weekly-2026-MM-DD.xlsx` 다운로드 확인
8. (선택) 다운로드된 xlsx를 Excel에서 열어 "일별" 또는 "주차별" 시트명과 데이터 확인
  </how-to-verify>
  <resume-signal>
검증 완료 후 "approved" 입력 또는 발견된 문제점 설명
  </resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 타입 오류 없음
2. `npm run build` — 프로덕션 빌드 성공 (SSR 환경 Blob/document 오류 없음)
3. 브라우저: 대시보드 헤더에 CSV + Excel 버튼 표시
4. CSV 다운로드: 파일명 형식 daily/weekly-YYYY-MM-DD.csv, BOM 포함 한국어 정상
5. Excel 다운로드: 파일명 형식 daily/weekly-YYYY-MM-DD.xlsx, 시트명 일별/주차별
6. 필터된 데이터만 내보내짐 (전체 원본 data가 아닌 filteredData 기준)
</verification>

<success_criteria>
- EXPO-01: CSV 다운로드 버튼이 작동하고 현재 필터링된 데이터를 .csv로 즉시 다운로드한다
- EXPO-02: Excel 다운로드 버튼이 작동하고 현재 필터링된 데이터를 .xlsx로 즉시 다운로드한다
- EXPO-03: 파일명에 현재 탭(daily/weekly)과 날짜(YYYY-MM-DD)가 포함된다
- 브라우저 검증 통과 (checkpoint:human-verify 승인)
</success_criteria>

<output>
완료 후 `.planning/phases/07-export/07-02-SUMMARY.md` 생성
</output>
