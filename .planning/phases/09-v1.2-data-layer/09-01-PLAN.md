---
phase: 09-v1.2-data-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - types/dashboard.ts
  - lib/mock-data.ts
autonomous: true
requirements:
  - CTYPE-01
  - CTYPE-02
  - REV-01
  - REV-02
  - COST-01
  - COST-02

must_haves:
  truths:
    - "types/dashboard.ts에 CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow 타입이 export되어 있다"
    - "TeamDashboardData에 customerTypeDaily, customerTypeWeekly, revenueBreakdownDaily, revenueBreakdownWeekly, costBreakdownDaily, costBreakdownWeekly 6개 배열 필드가 있다"
    - "lib/mock-data.ts의 mockTeamDashboardData가 확장된 TeamDashboardData를 완전히 만족한다"
    - "npm run build가 타입 오류 없이 통과한다"
  artifacts:
    - path: "types/dashboard.ts"
      provides: "CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow 타입 + 확장된 TeamDashboardData"
      exports:
        - CustomerTypeRow
        - RevenueBreakdownRow
        - CostBreakdownRow
        - TeamDashboardData
    - path: "lib/mock-data.ts"
      provides: "6개 새 배열 필드가 추가된 mockTeamDashboardData (0 플레이스홀더)"
      contains: "customerTypeDaily: []"
  key_links:
    - from: "lib/mock-data.ts"
      to: "types/dashboard.ts"
      via: "import type { TeamDashboardData }"
      pattern: "TeamDashboardData"
    - from: "lib/data.ts (Phase 02에서)"
      to: "types/dashboard.ts"
      via: "import type { CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow }"
      pattern: "CustomerTypeRow|RevenueBreakdownRow|CostBreakdownRow"
---

<objective>
Phase 9의 타입 컨트랙트와 Mock 데이터를 정의한다. 이 Plan은 후속 Plan 02(파서 구현)가 의존하는 타입 인터페이스를 먼저 확립한다.

Purpose: Phase 10/11/12 UI 단계가 의존할 TypeScript 인터페이스를 완성하고, mock 데이터를 업데이트하여 기존 빌드가 중단되지 않도록 한다.
Output: 확장된 types/dashboard.ts + 업데이트된 lib/mock-data.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 현재 types/dashboard.ts 전체 구조 — 이 파일을 확장한다 -->

```typescript
// types/dashboard.ts (현재)
export interface DailyRecord {
  date: string;
  revenue: number;
  profit: number;
  usageHours: number;
  usageCount: number;
  utilizationRate: number;
}

export interface WeeklyRecord {
  week: string;
  revenue: number;
  profit: number;
  usageHours: number;
  usageCount: number;
  utilizationRate: number;
  weeklyTarget: number;
}

export interface TeamDashboardData {
  daily: DailyRecord[];
  weekly: WeeklyRecord[];
  fetchedAt: string;
}
```

<!-- 현재 lib/mock-data.ts 구조 — mockTeamDashboardData를 확장한다 -->
```typescript
// lib/mock-data.ts (현재)
export const mockTeamDashboardData: TeamDashboardData = {
  daily: mockDailyRecords,
  weekly: mockWeeklyRecords,
  fetchedAt: "2026-03-08T00:00:00.000Z",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 정의 — CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow, TeamDashboardData 확장</name>
  <files>types/dashboard.ts</files>
  <action>
기존 types/dashboard.ts 파일에 다음 타입을 추가한다. DailyRecord / WeeklyRecord는 변경하지 않는다.

추가할 타입 3개:

```typescript
/** 고객 유형별 이용 건수 — 일별/주차별 공용 */
export interface CustomerTypeRow {
  date?: string;           // 일별 시트용 (YYYY-MM-DD). 주차별이면 undefined
  week?: string;           // 주차별 시트용. 일별이면 undefined
  roundTripCount: number;  // 왕복_건수
  callCount: number;       // 부름_건수
  oneWayCount: number;     // 편도_건수
}

/** 매출 세분화 한 행 — [d] raw / [w] raw 시트 */
export interface RevenueBreakdownRow {
  date: string;           // 일자 (ISO YYYY-MM-DD)
  rentalRevenue: number;  // 대여매출
  pfRevenue: number;      // PF매출
  drivingRevenue: number; // 주행매출
  callRevenue: number;    // 부름매출
  otherRevenue: number;   // 기타매출
}

/** 비용 분석 한 행 — 카테고리 합계 + 세부 드릴다운 포함 */
export interface CostBreakdownRow {
  date: string;                     // 일자 (ISO YYYY-MM-DD)
  transportCost: number;            // 운반비 (카테고리 합계)
  fuelCost: number;                 // 유류비
  parkingCost: number;              // 주차료
  inspectionCost: number;           // 점검비
  depreciationCost: number;         // 감가상각비
  commissionCost: number;           // 수수료
  chargeTransportCost: number;      // 충전운반비 (드릴다운 세부)
  callTransportCost: number;        // 부름운반비 (드릴다운 세부)
  zoneOneWayTransportCost: number;  // 존편도운반비 (드릴다운 세부)
}
```

기존 TeamDashboardData 인터페이스를 다음과 같이 교체한다 (fetchedAt 포함 전체 재정의):

```typescript
/** 대시보드 전체 데이터 컨테이너 */
export interface TeamDashboardData {
  daily: DailyRecord[];
  weekly: WeeklyRecord[];
  // Phase 9 신규 추가 — 고객유형/매출세분화/비용분석 데이터 레이어
  customerTypeDaily: CustomerTypeRow[];
  customerTypeWeekly: CustomerTypeRow[];
  revenueBreakdownDaily: RevenueBreakdownRow[];
  revenueBreakdownWeekly: RevenueBreakdownRow[];
  costBreakdownDaily: CostBreakdownRow[];
  costBreakdownWeekly: CostBreakdownRow[];
  fetchedAt: string;
}
```

주석 스타일은 기존 파일 형식(한국어 주석, JSDoc) 유지.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>types/dashboard.ts에 3개 타입이 export되고, TeamDashboardData에 6개 새 배열 필드가 있다. tsc --noEmit가 오류를 보고하지 않는다 (lib/mock-data.ts를 수정하기 전까지는 mock 오류가 예상되므로 두 Task를 연속 실행 후 최종 검증).</done>
</task>

<task type="auto">
  <name>Task 2: Mock 데이터 확장 — 6개 빈 배열 플레이스홀더 추가</name>
  <files>lib/mock-data.ts</files>
  <action>
lib/mock-data.ts의 mockTeamDashboardData 객체에 Phase 9에서 추가된 6개 배열 필드를 추가한다. 모두 빈 배열(`[]`)로 설정한다 (0 플레이스홀더 — UI 레이아웃 확인용, 실제 Google Sheets 연결 후 자동으로 실제 데이터로 교체됨).

import 라인에 새 타입 3개를 추가한다:
```typescript
import type { DailyRecord, WeeklyRecord, TeamDashboardData, CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow } from "@/types/dashboard";
```

mockTeamDashboardData 객체:
```typescript
export const mockTeamDashboardData: TeamDashboardData = {
  daily: mockDailyRecords,
  weekly: mockWeeklyRecords,
  // Phase 9 신규 — 0 플레이스홀더 (UI 레이아웃 확인용, Sheets 연결 후 실제 데이터로 대체)
  customerTypeDaily: [],
  customerTypeWeekly: [],
  revenueBreakdownDaily: [],
  revenueBreakdownWeekly: [],
  costBreakdownDaily: [],
  costBreakdownWeekly: [],
  fetchedAt: "2026-03-08T00:00:00.000Z",
};
```

mockDailyRecords, mockWeeklyRecords 배열과 다른 export 상수들은 변경하지 않는다.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build가 타입 오류 없이 통과한다. mockTeamDashboardData가 확장된 TeamDashboardData를 완전히 만족하고 기존 컨슈머(app/(dashboard)/dashboard/page.tsx 등)에서 오류가 없다.</done>
</task>

</tasks>

<verification>
두 Task 완료 후:
1. `npx tsc --noEmit` — 타입 오류 없음
2. `npm run build` — 빌드 성공
3. types/dashboard.ts에 CustomerTypeRow, RevenueBreakdownRow, CostBreakdownRow export 확인
4. TeamDashboardData에 6개 새 배열 필드 포함 확인
5. lib/mock-data.ts의 mockTeamDashboardData에 6개 빈 배열 필드 포함 확인
</verification>

<success_criteria>
- types/dashboard.ts: 3개 신규 타입 + 확장된 TeamDashboardData (6개 배열)
- lib/mock-data.ts: 6개 빈 배열 플레이스홀더 포함
- npm run build: 타입 오류 없이 통과
- 기존 대시보드 기능 중단 없음 (DailyRecord, WeeklyRecord 변경 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/09-v1.2-data-layer/09-01-SUMMARY.md`를 생성한다.
</output>
