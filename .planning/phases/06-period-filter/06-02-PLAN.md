---
phase: 06-period-filter
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - components/dashboard/period-filter.tsx
  - components/dashboard/dashboard-header.tsx
  - components/dashboard/kpi-cards.tsx
  - components/dashboard/data-table.tsx
  - components/dashboard/charts/charts-section.tsx
autonomous: true
requirements:
  - FILT-01

must_haves:
  truths:
    - "PeriodFilter 컴포넌트가 periods 배열의 버튼을 렌더링하고, 활성 기간은 variant='default', 비활성은 variant='outline'으로 표시된다"
    - "DashboardHeader 컴포넌트가 탭 전환 버튼(일별/주차별)과 기간 토글 버튼을 한 행에 좌우 정렬로 표시한다"
    - "KpiCards, DataTable, ChartsSection이 'use client' 지시자를 가져 DashboardContent(Client Component) 하위에서 직접 사용 가능하다"
  artifacts:
    - path: "components/dashboard/period-filter.tsx"
      provides: "기간 토글 버튼 UI — shadcn Button 기반"
      exports: ["PeriodFilter"]
    - path: "components/dashboard/dashboard-header.tsx"
      provides: "탭 전환 + 기간 필터를 한 행에 배치하는 헤더"
      exports: ["DashboardHeader"]
    - path: "components/dashboard/kpi-cards.tsx"
      provides: "KPI 카드 그리드 (Client Component로 전환)"
    - path: "components/dashboard/data-table.tsx"
      provides: "데이터 테이블 (Client Component로 전환)"
    - path: "components/dashboard/charts/charts-section.tsx"
      provides: "차트 섹션 (Client Component로 전환)"
  key_links:
    - from: "components/dashboard/dashboard-header.tsx"
      to: "components/dashboard/period-filter.tsx"
      via: "import PeriodFilter"
      pattern: "from './period-filter'"
    - from: "components/dashboard/dashboard-header.tsx"
      to: "lib/period-utils.ts"
      via: "import DAILY_PERIODS, WEEKLY_PERIODS"
      pattern: "from '@/lib/period-utils'"
---

<objective>
기간 선택 UI 컴포넌트를 생성하고, 기존 Server Component 3개에 'use client'를 추가하여 이후 DashboardContent Client Component에서 사용할 수 있도록 준비한다.

Purpose: FILT-01(기간 선택 UI)를 위한 컴포넌트 레이어 구성.
Output: period-filter.tsx, dashboard-header.tsx (신규), kpi-cards/data-table/charts-section (Client Component 전환)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-period-filter/06-CONTEXT.md
@.planning/phases/06-period-filter/06-RESEARCH.md
@.planning/phases/06-period-filter/06-01-SUMMARY.md

<interfaces>
<!-- lib/period-utils.ts에서 export된 계약 (Plan 01에서 생성) -->
```typescript
export type PeriodKey = 'this-week' | 'last-week' | 'this-month' | 'last-month';

export const DAILY_PERIODS: PeriodKey[] = ['this-week', 'last-week', 'this-month', 'last-month'];
export const WEEKLY_PERIODS: PeriodKey[] = ['this-month', 'last-month'];

export const PERIOD_LABELS: Record<PeriodKey, string> = {
  'this-week': '이번 주',
  'last-week': '지난 주',
  'this-month': '이번 달',
  'last-month': '지난 달',
};

export const DEFAULT_DAILY_PERIOD: PeriodKey = 'this-month';
export const DEFAULT_WEEKLY_PERIOD: PeriodKey = 'this-month';
```

<!-- 기존 컴포넌트 props 인터페이스 (변경 없음, 참고용) -->
```typescript
// kpi-cards.tsx
interface KpiCardsProps { data: TeamDashboardData; tab: 'daily' | 'weekly'; }

// data-table.tsx
interface DataTableProps { data: TeamDashboardData; tab: 'daily' | 'weekly'; }

// charts-section.tsx
interface ChartsSectionProps { data: TeamDashboardData; tab: 'daily' | 'weekly'; }

// tab-nav.tsx (DashboardHeader로 대체됨 — 기존 URL 동기화 패턴 참고)
// useRouter, useSearchParams, usePathname 조합으로 params.set('tab', value) 후 router.replace
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PeriodFilter + DashboardHeader 신규 컴포넌트 생성</name>
  <files>
    components/dashboard/period-filter.tsx,
    components/dashboard/dashboard-header.tsx
  </files>
  <action>
    **period-filter.tsx 생성:**
    - 'use client' 지시자
    - shadcn Button import: `import { Button } from '@/components/ui/button'`
    - PeriodKey, PERIOD_LABELS import from '@/lib/period-utils'
    - Props: `{ periods: PeriodKey[]; active: PeriodKey; onChange: (p: PeriodKey) => void }`
    - 렌더링: `<div className="flex gap-1">` 안에 periods.map → Button
      - active === p: variant="default"
      - 그 외: variant="outline"
      - size="sm"
      - onClick: () => onChange(p)
      - 텍스트: PERIOD_LABELS[p]

    **dashboard-header.tsx 생성:**
    - 'use client' 지시자
    - useRouter, useSearchParams, usePathname import from 'next/navigation'
    - Tabs, TabsList, TabsTrigger import from '@/components/ui/tabs'
    - PeriodFilter import from './period-filter'
    - PeriodKey, DAILY_PERIODS, WEEKLY_PERIODS, DEFAULT_DAILY_PERIOD, DEFAULT_WEEKLY_PERIOD import from '@/lib/period-utils'
    - Props: `{ tab: 'daily' | 'weekly'; period: PeriodKey; onPeriodChange: (p: PeriodKey) => void }`
    - handleTabChange(value: string):
      - `const params = new URLSearchParams(searchParams.toString())`
      - `params.set('tab', value)`
      - 탭 전환 시 period 리셋: `params.set('period', value === 'daily' ? DEFAULT_DAILY_PERIOD : DEFAULT_WEEKLY_PERIOD)`
      - `router.replace(\`\${pathname}?\${params.toString()}\`, { scroll: false })`
    - 렌더링: `<div className="flex items-center justify-between">`
      - 왼쪽: `<Tabs value={tab} onValueChange={handleTabChange}><TabsList><TabsTrigger value="daily">일별</TabsTrigger><TabsTrigger value="weekly">주차별</TabsTrigger></TabsList></Tabs>`
      - 오른쪽: `<PeriodFilter periods={tab === 'daily' ? DAILY_PERIODS : WEEKLY_PERIODS} active={period} onChange={onPeriodChange} />`

    코드 주석은 한국어로 작성한다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    period-filter.tsx와 dashboard-header.tsx 파일이 존재하고 TypeScript 오류 없이 빌드된다.
    DashboardHeader가 탭 전환 시 period를 해당 탭 기본값으로 리셋하는 handleTabChange를 포함한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: KpiCards, DataTable, ChartsSection에 'use client' 추가</name>
  <files>
    components/dashboard/kpi-cards.tsx,
    components/dashboard/data-table.tsx,
    components/dashboard/charts/charts-section.tsx
  </files>
  <action>
    세 파일 각각의 최상단에 `'use client';` 지시자를 추가한다.

    **kpi-cards.tsx:**
    - 파일 첫 줄에 `'use client';` 추가 (기존 주석 위 또는 아래)
    - 나머지 코드 변경 없음

    **data-table.tsx:**
    - 파일 첫 줄에 `'use client';` 추가
    - 나머지 코드 변경 없음

    **charts/charts-section.tsx:**
    - 이미 'use client'가 있는지 먼저 확인한다
    - 없으면 첫 줄에 추가
    - 나머지 코드 변경 없음

    주의: 이 컴포넌트들은 데이터 fetch 없이 순수 렌더링만 하므로 Client Component 전환에 문제 없다.
    내부 로직, props 인터페이스, 렌더링 로직은 일절 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    kpi-cards.tsx, data-table.tsx, charts/charts-section.tsx 각각의 첫 줄(또는 주석 바로 다음 줄)에 'use client'가 존재한다.
    빌드 오류 없음.
  </done>
</task>

</tasks>

<verification>
- components/dashboard/period-filter.tsx 존재
- components/dashboard/dashboard-header.tsx 존재
- kpi-cards.tsx, data-table.tsx, charts-section.tsx 각각 'use client' 보유
- npm run build 성공
- npm run lint 통과
</verification>

<success_criteria>
PeriodFilter 컴포넌트가 periods 배열을 shadcn Button으로 렌더링한다.
DashboardHeader가 탭 + 기간 필터를 justify-between 한 행에 배치한다.
3개 기존 컴포넌트가 'use client'를 보유하여 Client Component 트리에서 사용 가능하다.
</success_criteria>

<output>
완료 후 `.planning/phases/06-period-filter/06-02-SUMMARY.md` 생성
</output>
