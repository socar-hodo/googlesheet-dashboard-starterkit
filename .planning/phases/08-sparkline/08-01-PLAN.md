---
phase: 08-sparkline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/kpi-card.tsx
  - components/dashboard/kpi-cards.tsx
autonomous: true
requirements:
  - SPRK-01
  - SPRK-02

must_haves:
  truths:
    - "5개 KPI 카드 각각에 미니 스파크라인 AreaChart가 렌더링된다"
    - "스파크라인은 축/툴팁/라벨 없이 순수 트렌드 선만 표시된다"
    - "daily 탭에서 최근 7일, weekly 탭에서 최근 8주 데이터 포인트를 표시한다"
    - "데이터가 2개 미만이면 스파크라인이 렌더링되지 않고 카드 레이아웃이 정상 유지된다"
    - "스파크라인 색상이 var(--chart-1) CSS 변수를 통해 다크/라이트 테마 전환에 자동 대응된다"
  artifacts:
    - path: "components/dashboard/kpi-card.tsx"
      provides: "sparklineData?: number[] prop 추가, AreaChart 조건부 렌더링, 'use client' 선언"
      contains: "'use client'"
    - path: "components/dashboard/kpi-cards.tsx"
      provides: "5개 KPI별 sparklineData 추출 및 KpiCard에 전달"
      contains: "sparklineData"
  key_links:
    - from: "components/dashboard/kpi-cards.tsx"
      to: "components/dashboard/kpi-card.tsx"
      via: "sparklineData prop"
      pattern: "sparklineData=\\{.*\\.map\\("
    - from: "components/dashboard/kpi-card.tsx"
      to: "Recharts AreaChart"
      via: "var(--chart-1) CSS 변수"
      pattern: "stroke=\"var\\(--chart-1\\)\""
---

<objective>
KPI 카드 각각에 미니 스파크라인 차트를 추가한다.

Purpose: 매출 등 5개 KPI의 최근 추이를 숫자 하나가 아닌 시각적 트렌드 선으로 즉시 파악할 수 있게 한다. 신규 API 호출 없이 기존 데이터에서 파생한다.
Output: kpi-card.tsx에 AreaChart 스파크라인 렌더링, kpi-cards.tsx에서 5개 KPI × 2탭(daily/weekly) sparklineData 추출 및 전달.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sparkline/08-CONTEXT.md
@.planning/phases/08-sparkline/08-RESEARCH.md
</context>

<interfaces>
<!-- 현재 kpi-card.tsx 인터페이스 (수정 대상) -->

```typescript
// components/dashboard/kpi-card.tsx — 현재 상태 (Server Component, 'use client' 없음)
interface KpiCardProps {
  title: string;            // KPI 명칭 (예: "매출")
  value: string;            // 포맷된 실적값 (예: "₩1,234만")
  target?: string;          // 포맷된 목표값 (Weekly 전용)
  achievementRate?: number; // 달성률 % (Weekly 전용)
  deltaText?: string;       // 포맷된 델타 문자열
  deltaColorClass?: string; // 델타 색상 클래스
  icon: React.ReactNode;    // lucide-react 아이콘
}
// 추가할 prop: sparklineData?: number[]
```

<!-- 현재 kpi-cards.tsx 핵심 구조 (수정 대상) -->

```typescript
// components/dashboard/kpi-cards.tsx — 현재 구조 요약
'use client';

export function KpiCards({ data, tab }: KpiCardsProps) {
  if (tab === 'daily') {
    const sorted = [...data.daily].sort((a, b) => a.date.localeCompare(b.date));
    const current = sorted[sorted.length - 1];
    const previous = sorted[sorted.length - 2];
    // cards 배열 정의 후 KpiCard 렌더링
    // sparklineData 미전달 상태 — 추가 필요
  }

  // weekly 탭
  const current = data.weekly[data.weekly.length - 1];
  // weekly sorted 없음 — data.weekly 직접 사용
  // sparklineData 미전달 상태 — 추가 필요
}
```

<!-- DailyRecord, WeeklyRecord 타입 필드 (sparklineData 추출에 사용) -->

```typescript
// types/dashboard.ts (참고용 — 수정 불필요)
interface DailyRecord {
  date: string;
  revenue: number;
  profit: number;
  usageCount: number;
  utilizationRate: number;
  usageHours: number;
}

interface WeeklyRecord {
  week: string;
  revenue: number;
  weeklyTarget: number;
  profit: number;
  usageCount: number;
  utilizationRate: number;
  usageHours: number;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: kpi-card.tsx — sparklineData prop 추가 및 AreaChart 렌더링</name>
  <files>components/dashboard/kpi-card.tsx</files>
  <action>
    파일 상단에 `'use client';` 추가 (Recharts는 브라우저 DOM 필수, Server Component에서 사용 불가).

    import에 Recharts 추가:
    ```typescript
    import { AreaChart, Area, ResponsiveContainer } from 'recharts';
    ```

    KpiCardProps 인터페이스에 마지막 prop 추가:
    ```typescript
    sparklineData?: number[];  // 스파크라인 데이터 포인트 배열
    ```

    함수 시그니처 비구조화에 `sparklineData` 추가.

    CardContent 내부 — 기존 `deltaText` 렌더링 블록 아래에 스파크라인 블록 추가:
    ```typescript
    {sparklineData && sparklineData.length >= 2 && (
      <div className="pt-1">
        <ResponsiveContainer width="100%" height={40} minWidth={0}>
          <AreaChart
            data={sparklineData.map((v, i) => ({ v, i }))}
            margin={{ top: 2, right: 0, left: 0, bottom: 2 }}
          >
            <Area
              type="monotone"
              dataKey="v"
              stroke="var(--chart-1)"
              strokeWidth={1.5}
              fill="var(--chart-1)"
              fillOpacity={0.15}
              dot={false}
              isAnimationActive={false}
            />
          </AreaChart>
        </ResponsiveContainer>
      </div>
    )}
    ```

    중요 규칙:
    - XAxis, YAxis, CartesianGrid, Tooltip, Legend는 절대 포함하지 않는다 (40px 높이에서 노이즈)
    - `sparklineData.length >= 2` 조건부 렌더링 필수 (1개 이하면 Recharts 경고 발생)
    - `isAnimationActive={false}` 명시 (여러 카드 동시 렌더링 시 시각적 안정성)
    - CSS 변수 `var(--chart-1)` 직접 사용 — useTheme import 불필요
    - 기존 achievementRate, target, deltaText 렌더링 로직은 변경하지 않는다
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>kpi-card.tsx가 'use client' 선언을 갖고, sparklineData prop을 받아 AreaChart를 조건부 렌더링하며, npm run build가 에러 없이 통과한다</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: kpi-cards.tsx — 5개 KPI × 2탭 sparklineData 추출 및 전달</name>
  <files>components/dashboard/kpi-cards.tsx</files>
  <action>
    **Daily 탭 블록 수정:**

    기존 `sorted` 변수 아래에 상수 추가:
    ```typescript
    const DAILY_N = 7; // 최근 7일
    ```

    cards 배열 각 항목에 `sparklineData` 필드 추가:
    ```typescript
    // 매출
    sparklineData: sorted.map(d => d.revenue).slice(-DAILY_N),

    // GPM (수익률 %)
    sparklineData: sorted
      .map(d => d.revenue > 0 ? (d.profit / d.revenue) * 100 : 0)
      .slice(-DAILY_N),

    // 이용건수
    sparklineData: sorted.map(d => d.usageCount).slice(-DAILY_N),

    // 가동률
    sparklineData: sorted.map(d => d.utilizationRate).slice(-DAILY_N),

    // 이용시간
    sparklineData: sorted.map(d => d.usageHours).slice(-DAILY_N),
    ```

    KpiCard 렌더링에 `sparklineData={card.sparklineData}` prop 추가.

    **Weekly 탭 블록 수정:**

    weekly는 data.weekly를 직접 사용하지만, 스파크라인의 시간순 정렬을 보장하기 위해 sorted 배열 생성:
    ```typescript
    const weeklySorted = [...data.weekly].sort((a, b) => a.week.localeCompare(b.week));
    const WEEKLY_N = 8; // 최근 8주
    ```

    weekly cards 배열 각 항목에 `sparklineData` 필드 추가:
    ```typescript
    // 매출
    sparklineData: weeklySorted.map(d => d.revenue).slice(-WEEKLY_N),

    // GPM
    sparklineData: weeklySorted
      .map(d => d.revenue > 0 ? (d.profit / d.revenue) * 100 : 0)
      .slice(-WEEKLY_N),

    // 이용건수
    sparklineData: weeklySorted.map(d => d.usageCount).slice(-WEEKLY_N),

    // 가동률
    sparklineData: weeklySorted.map(d => d.utilizationRate).slice(-WEEKLY_N),

    // 이용시간
    sparklineData: weeklySorted.map(d => d.usageHours).slice(-WEEKLY_N),
    ```

    weekly KpiCard 렌더링에도 `sparklineData={card.sparklineData}` prop 추가.

    중요 규칙:
    - 기존 `sorted`, `current`, `previous` 변수는 그대로 유지 — 재계산하지 않는다
    - weekly에서 `current`/`previous` 추출은 기존 방식(`data.weekly[data.weekly.length - 1]`) 유지
    - weeklySorted는 sparklineData 전용으로만 사용
    - TypeScript 타입 오류가 없어야 한다 — card 객체에 sparklineData 필드 추가 시 타입 추론이 자동으로 처리됨
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>kpi-cards.tsx가 daily/weekly 양쪽에서 5개 KPI 모두 sparklineData를 계산하여 KpiCard에 전달하며, npm run build가 타입 에러 없이 통과한다</done>
</task>

</tasks>

<verification>
두 Task 완료 후 전체 검증:
1. `npm run build` — TypeScript 컴파일 및 Next.js 빌드 에러 없음
2. `npm run lint` — ESLint 경고/에러 없음
3. kpi-card.tsx 첫 줄에 `'use client'` 존재 확인
4. kpi-cards.tsx에 `sparklineData` prop 전달 코드 5개 × 2탭 = 10곳 존재 확인
</verification>

<success_criteria>
- npm run build 에러 없이 통과
- npm run lint 에러 없이 통과
- kpi-card.tsx: 'use client' 선언, sparklineData prop, AreaChart 렌더링 코드 포함
- kpi-cards.tsx: daily 7일, weekly 8주 sparklineData 추출 및 KpiCard에 전달
- 데이터 2개 미만 시 조건부 렌더링으로 레이아웃 보호
</success_criteria>

<output>
완료 후 `.planning/phases/08-sparkline/08-01-SUMMARY.md` 생성:
- 실제 변경된 코드 패턴 기록
- 빌드/린트 결과
- 발견된 이슈 및 해결 방법
</output>
