---
phase: 10-customer-type-analysis
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - components/dashboard/charts/customer-type-donut.tsx
  - components/dashboard/charts/customer-type-trend.tsx
  - components/dashboard/charts/customer-type-section.tsx
  - components/dashboard/charts/charts-section.tsx
  - components/dashboard/dashboard-content.tsx
autonomous: false
requirements:
  - CTYPE-01
  - CTYPE-02
  - CTYPE-03
must_haves:
  truths:
    - "대시보드 기존 4개 차트 아래에 도넛 차트(고객 유형 분포)와 스택 바 차트(유형별 이용건수 추이)가 나란히 표시된다"
    - "도넛 차트 중앙에 '총 N건' 텍스트가 표시되고, 범례로만 왕복/부름/편도 유형명이 나타난다"
    - "스택 바 차트 Y축에 '건' 단위가 표시되고, 3색 바가 누적된다"
    - "기간 필터 토글 변경 시 두 차트 모두 해당 기간 데이터로 즉시 갱신된다"
    - "일별 탭: 날짜(2/1 형식), 주차별 탭: 주차(1주 형식) X축으로 전환된다"
  artifacts:
    - path: "components/dashboard/charts/customer-type-donut.tsx"
      provides: "CustomerTypeDonut 컴포넌트 — PieChart, 중앙 총건수 텍스트, Legend만"
      exports: ["CustomerTypeDonut"]
    - path: "components/dashboard/charts/customer-type-trend.tsx"
      provides: "CustomerTypeTrend 컴포넌트 — stacked BarChart, Y축 건 단위"
      exports: ["CustomerTypeTrend"]
    - path: "components/dashboard/charts/customer-type-section.tsx"
      provides: "CustomerTypeSection 래퍼 — 도넛(1/3) + 추이(2/3) 그리드"
      exports: ["CustomerTypeSection"]
    - path: "components/dashboard/charts/charts-section.tsx"
      provides: "CustomerTypeSection import 및 ChartsSection 하단 렌더링"
      contains: "CustomerTypeSection"
    - path: "components/dashboard/dashboard-content.tsx"
      provides: "CustomerTypeSection에 filteredData.customerTypeDaily/Weekly 전달"
      contains: "CustomerTypeSection"
  key_links:
    - from: "components/dashboard/charts/charts-section.tsx"
      to: "components/dashboard/charts/customer-type-section.tsx"
      via: "import + JSX 렌더링"
      pattern: "CustomerTypeSection"
    - from: "components/dashboard/charts/customer-type-donut.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "getChartColors — colors.chart3 사용"
      pattern: "colors.chart3"
    - from: "components/dashboard/charts/customer-type-trend.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "getChartColors — colors.chart3 사용"
      pattern: "colors.chart3"
---

<objective>
Plan 02는 고객 유형 분석 UI를 완성한다: 도넛 차트, 스택 바 차트, 섹션 래퍼를 새로 생성하고 대시보드에 연결한다.

Purpose: CTYPE-01 (도넛), CTYPE-02 (스택 바 추이), CTYPE-03 (기간 연동)를 가시적으로 완성한다.
Output: customer-type-donut.tsx, customer-type-trend.tsx, customer-type-section.tsx (신규) + charts-section.tsx, dashboard-content.tsx (수정)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-customer-type-analysis/10-CONTEXT.md
@.planning/phases/10-customer-type-analysis/10-RESEARCH.md
@.planning/phases/10-customer-type-analysis/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01이 준비한 인터페이스 + 기존 패턴 — 탐색 불필요 -->

From types/dashboard.ts:
```typescript
export interface CustomerTypeRow {
  date?: string;           // 일별 (YYYY-MM-DD)
  week?: string;           // 주차별 ("N월 N주차")
  roundTripCount: number;  // 왕복_건수
  callCount: number;       // 부름_건수
  oneWayCount: number;     // 편도_건수
}

export interface TeamDashboardData {
  daily: DailyRecord[];
  weekly: WeeklyRecord[];
  customerTypeDaily: CustomerTypeRow[];
  customerTypeWeekly: CustomerTypeRow[];
  // ...
}
```

From components/dashboard/charts/chart-colors.ts (Plan 01 후 상태):
```typescript
export interface ChartColorMode {
  chart1: string;
  chart2: string;
  chart3: string;  // 편도 — Plan 01에서 추가됨
  chart4: string;
  chart5: string;
  profitPositive: string;
  // ...
}
export function getChartColors(isDark: boolean): ChartColorMode
```

From components/dashboard/charts/charts-section.tsx (현재 구조):
```typescript
export function ChartsSection({ data, tab }: ChartsSectionProps) {
  const records: DailyRecord[] | WeeklyRecord[] = ...
  return (
    <div className="space-y-6">
      <RevenueTrendChart records={records} tab={tab} />
      <ProfitTrendChart records={records} tab={tab} />
      <UtilizationTrendChart records={records} tab={tab} />
      <UsageTrendChart records={records} tab={tab} />
      {/* 여기 아래에 CustomerTypeSection 추가 */}
    </div>
  );
}
```

From components/dashboard/dashboard-content.tsx (Plan 01 후 — filteredData):
```typescript
// filteredData가 이미 customerTypeDaily/Weekly를 필터링해서 반환함
<ChartsSection data={filteredData} tab={tab} />
```

기존 차트 컴포넌트 패턴 (revenue-trend-chart.tsx 기준):
```typescript
"use client";
import { ResponsiveContainer, ... } from "recharts";
import { useTheme } from "next-themes";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { getChartColors } from "./chart-colors";

export function SomeChart({ data, tab }: Props) {
  const { resolvedTheme } = useTheme();
  const colors = getChartColors(resolvedTheme === "dark");
  return (
    <Card>
      <CardHeader><CardTitle>제목</CardTitle></CardHeader>
      <CardContent>
        <ResponsiveContainer width="100%" height={280} minWidth={0}>
          {/* 차트 */}
        </ResponsiveContainer>
      </CardContent>
    </Card>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CustomerTypeDonut 컴포넌트 생성 (CTYPE-01)</name>
  <files>components/dashboard/charts/customer-type-donut.tsx</files>
  <action>
신규 파일 `components/dashboard/charts/customer-type-donut.tsx`를 생성한다.

**컴포넌트 요건 (CONTEXT.md 결정 사항):**
- Card > CardHeader > CardTitle("고객 유형 분포") > CardContent 패턴
- Recharts `PieChart > Pie`, `innerRadius="60%"`, `outerRadius="80%"`
- `label={false}`, `labelLine={false}` — 조각 레이블 없음
- 세 `Cell` 컴포넌트: chart1(왕복), chart2(부름), chart3(편도) 순서
- `Legend` 컴포넌트로만 유형명 표시
- 중앙 총건수 텍스트: PieChart 내부 SVG `<text x="50%" y="50%">총 {total}건</text>`
  - `textAnchor="middle"`, `dominantBaseline="middle"`, fill=colors.axis, fontSize="14px", fontWeight="600"
  - x="50%", y="50%" 사용 (픽셀 좌표 금지 — ResponsiveContainer에서 깨짐)
- 툴팁: `formatter={(value, name) => [`${value}건 (${pct}%)`, name]}`
  - contentStyle: backgroundColor=colors.tooltip.bg, border, borderRadius="8px", fontSize="12px"
- data prop이 빈 배열일 때 empty state 렌더링: "데이터 없음" 텍스트를 Card 내부 중앙에 표시
- 도넛 집계 로직: CustomerTypeRow[] 전체를 reduce로 합산 → 단일 총계 pieData 생성

```typescript
interface CustomerTypeDonutProps {
  data: CustomerTypeRow[];
}
```

**주의:**
- `<text>` 요소는 `<Pie>` 내부가 아니라 `<PieChart>` 직계 자식으로 위치
- total이 0이면 pct 계산 시 0으로 폴백 (나눗셈 에러 방지)
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | grep -E "error|Error|✓" | tail -10</automated>
  </verify>
  <done>빌드 통과. CustomerTypeDonut 컴포넌트가 export됨. CustomerTypeRow[] prop을 받아 PieChart 도넛을 렌더링. 중앙 총건수 텍스트, Legend, Tooltip 포함.</done>
</task>

<task type="auto">
  <name>Task 2: CustomerTypeTrend 컴포넌트 생성 (CTYPE-02)</name>
  <files>components/dashboard/charts/customer-type-trend.tsx</files>
  <action>
신규 파일 `components/dashboard/charts/customer-type-trend.tsx`를 생성한다.

**컴포넌트 요건 (CONTEXT.md 결정 사항):**
- Card > CardHeader > CardTitle("유형별 이용건수 추이") > CardContent 패턴
- Recharts `BarChart` with `CartesianGrid`, `XAxis`, `YAxis`, `Tooltip`, `Legend`, `ResponsiveContainer`
- 세 `Bar` 컴포넌트 모두 `stackId="a"` — 누적 스택
  - `<Bar dataKey="왕복" stackId="a" fill={colors.chart1} />`
  - `<Bar dataKey="부름" stackId="a" fill={colors.chart2} />`
  - `<Bar dataKey="편도" stackId="a" fill={colors.chart3} radius={[2, 2, 0, 0]} />` — radius는 최상단 바만
- Y축: `tickFormatter={(v) => `${v}건`}`, `width={50}`
- X축 레이블 포맷:
  - daily + date 있음: `"2026-03-01"` → `"3/1"` (월/일, parseInt로 앞 0 제거)
  - weekly + week 있음: `"2월 3주차"` → `"2월 3주"` (replace "주차" → "주")
- 툴팁: 기본 formatter `(value, name) => [\`${value}건\`, name]`
  - 합계 행은 커스텀 Tooltip content 함수로 구현:
    ```typescript
    const CustomTooltip = ({ active, payload, label }: TooltipProps<number, string>) => {
      if (!active || !payload?.length) return null;
      const total = payload.reduce((sum, p) => sum + (p.value as number), 0);
      return (
        <div style={{ backgroundColor: colors.tooltip.bg, border: `1px solid ${colors.tooltip.border}`, borderRadius: '8px', padding: '8px', fontSize: '12px' }}>
          <p style={{ marginBottom: 4 }}>{label}</p>
          {payload.map((p) => (
            <p key={p.dataKey} style={{ color: p.fill }}>{p.name}: {p.value}건</p>
          ))}
          <p style={{ borderTop: `1px solid ${colors.tooltip.border}`, marginTop: 4, paddingTop: 4 }}>합계: {total}건</p>
        </div>
      );
    };
    ```
  - `<Tooltip content={<CustomTooltip />} />` 사용
- data prop이 빈 배열일 때: "데이터 없음" Card 내부 텍스트
- BarChart margin: `{ top: 5, right: 20, left: 10, bottom: 5 }`

```typescript
interface CustomerTypeTrendProps {
  data: CustomerTypeRow[];
  tab: 'daily' | 'weekly';
}
```

**주의:**
- radius는 편도(최상단) Bar에만 적용. 왕복/부름에는 미적용 — 내부 세그먼트에 radius 적용 시 시각적 갭 발생
- CustomTooltip은 컴포넌트 내부에서 colors에 클로저 접근하거나 props로 전달
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | grep -E "error|Error|✓" | tail -10</automated>
  </verify>
  <done>빌드 통과. CustomerTypeTrend 컴포넌트가 export됨. stackId="a" 3개 Bar, Y축 건 단위, 커스텀 툴팁(합계 포함), 탭별 X축 레이블 포맷 적용.</done>
</task>

<task type="auto">
  <name>Task 3: CustomerTypeSection 생성 + ChartsSection 연결 + 브라우저 검증</name>
  <files>components/dashboard/charts/customer-type-section.tsx, components/dashboard/charts/charts-section.tsx</files>
  <action>
**customer-type-section.tsx 생성:**

```typescript
'use client';
// components/dashboard/charts/customer-type-section.tsx
import type { CustomerTypeRow } from '@/types/dashboard';
import { CustomerTypeDonut } from './customer-type-donut';
import { CustomerTypeTrend } from './customer-type-trend';

interface CustomerTypeSectionProps {
  daily: CustomerTypeRow[];
  weekly: CustomerTypeRow[];
  tab: 'daily' | 'weekly';
}

export function CustomerTypeSection({ daily, weekly, tab }: CustomerTypeSectionProps) {
  const data = tab === 'daily' ? daily : weekly;
  return (
    // 도넛(1/3 폭) + 추이(2/3 폭), 모바일에서 세로 단일 컬럼
    <div className="grid grid-cols-1 gap-6 md:grid-cols-[1fr_2fr]">
      <CustomerTypeDonut data={data} />
      <CustomerTypeTrend data={data} tab={tab} />
    </div>
  );
}
```

---

**charts-section.tsx 수정:**

1. import에 CustomerTypeSection 추가:
   ```typescript
   import { CustomerTypeSection } from './customer-type-section';
   ```

2. JSX 반환의 `<div className="space-y-6">` 내부 마지막에 CustomerTypeSection 추가:
   ```tsx
   <CustomerTypeSection
     daily={data.customerTypeDaily}
     weekly={data.customerTypeWeekly}
     tab={tab}
   />
   ```
   기존 4개 차트(`RevenueTrendChart`, `ProfitTrendChart`, `UtilizationTrendChart`, `UsageTrendChart`) 아래에 위치.

---

**dashboard-content.tsx 확인:**
ChartsSection에 `data={filteredData}`가 이미 전달되고 있으므로 (`<ChartsSection data={filteredData} tab={tab} />`), CustomerTypeSection은 자동으로 필터링된 customerTypeDaily/Weekly를 받는다. dashboard-content.tsx 수정 불필요 — 확인만 한다.

---

**브라우저 검증 (Playwright):**
개발 서버가 실행 중인지 확인 후 필요하면 `npm run dev &` 백그라운드 실행.

1. http://localhost:3000/dashboard 접속
2. 기존 4개 차트 아래에 "고객 유형 분포" 도넛 차트와 "유형별 이용건수 추이" 스택 바 차트가 나란히 렌더링되는지 스냅샷으로 확인
3. 기간 필터 "지난 달" 토글 클릭 → 도넛/추이 차트 데이터가 2026-02 행으로 갱신되는지 확인
4. "주차별" 탭 전환 → 추이 차트 X축이 "N월 N주" 형식으로 변경되는지 확인
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | grep -E "error|Error|✓" | tail -10</automated>
  </verify>
  <done>
  - 빌드 통과
  - Playwright 스냅샷에서 "고객 유형 분포" 카드와 "유형별 이용건수 추이" 카드가 기존 차트 아래 동일 행에 렌더링됨
  - 기간 필터 변경 시 두 차트 데이터 갱신 확인
  - 주차별 탭에서 X축 레이블이 주차 형식으로 표시됨
  </done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/User/hodo/sheet-dashboard && npm run build
```
빌드 통과 + Playwright 브라우저 검증으로 시각적 렌더링 확인.

CTYPE-01: 도넛 차트 — PieChart, 중앙 총건수, Legend, Tooltip(건수+%)
CTYPE-02: 스택 바 추이 — stackId="a", Y축 건 단위, 탭별 X축, 합계 툴팁
CTYPE-03: 기간 필터 연동 — filteredData를 통해 두 차트 모두 즉시 갱신
</verification>

<success_criteria>
- `npm run build` 에러 없음
- customer-type-donut.tsx, customer-type-trend.tsx, customer-type-section.tsx 신규 파일 존재
- charts-section.tsx에 CustomerTypeSection import 및 렌더링 포함
- 브라우저에서 기존 4개 차트 아래에 도넛 + 스택 바가 나란히 표시됨
- 기간 필터 "지난 달" 클릭 시 두 차트 데이터 변경 확인
- 주차별 탭에서 스택 바 X축이 주차 형식으로 표시됨
- 다크/라이트 테마 전환 시 차트 색상 변경 (chart1/2/3 css 변수 연동)
</success_criteria>

<output>
완료 후 `.planning/phases/10-customer-type-analysis/10-02-SUMMARY.md` 생성
</output>
