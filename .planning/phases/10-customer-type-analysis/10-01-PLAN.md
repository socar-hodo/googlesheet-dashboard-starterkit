---
phase: 10-customer-type-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/charts/chart-colors.ts
  - lib/mock-data.ts
  - components/dashboard/dashboard-content.tsx
  - lib/period-utils.ts
autonomous: true
requirements:
  - CTYPE-03
must_haves:
  truths:
    - "기간 필터 토글 변경 시 고객 유형 데이터가 필터링된다 (filteredData.customerTypeDaily/Weekly가 해당 기간 행만 포함)"
    - "개발 환경에서 mock 데이터로 도넛/추이 차트가 빈 상태 없이 렌더링된다"
    - "chart3/chart4/chart5 색상값이 ChartColorMode에 정의되어 getChartColors() 반환값에 포함된다"
  artifacts:
    - path: "components/dashboard/charts/chart-colors.ts"
      provides: "chart3, chart4, chart5 필드가 ChartColorMode 인터페이스와 CHART_COLORS 객체 양쪽에 추가됨"
      contains: "chart3"
    - path: "lib/mock-data.ts"
      provides: "mockCustomerTypeDaily (8개 행, 2026-02/03), mockCustomerTypeWeekly (4개 행, 2월/3월)"
      contains: "roundTripCount"
    - path: "components/dashboard/dashboard-content.tsx"
      provides: "filteredData useMemo가 customerTypeDaily/Weekly도 함께 필터링"
      contains: "customerTypeDaily: filtered"
    - path: "lib/period-utils.ts"
      provides: "filterCustomerTypeWeekly 헬퍼 함수 export"
      exports: ["filterCustomerTypeWeekly"]
  key_links:
    - from: "components/dashboard/dashboard-content.tsx"
      to: "lib/period-utils.ts"
      via: "filterCustomerTypeWeekly import"
      pattern: "filterCustomerTypeWeekly"
    - from: "components/dashboard/dashboard-content.tsx"
      to: "filteredData.customerTypeDaily"
      via: "useMemo 내 date range 필터"
      pattern: "customerTypeDaily: filtered"
---

<objective>
Plan 01은 고객 유형 차트가 기간 필터에 응답하기 위한 세 가지 인프라를 준비한다.

1. `chart-colors.ts` — chart3/4/5 색상 필드 추가 (Plan 02 차트 컴포넌트가 사용)
2. `mock-data.ts` — customerTypeDaily/Weekly 샘플 데이터 채우기 (빈 배열 → 실제 행)
3. `dashboard-content.tsx` + `period-utils.ts` — filteredData useMemo를 확장하여 customerType 배열도 기간 필터 적용

Purpose: Plan 02 차트 컴포넌트가 의존하는 색상 계약과 필터링 동작을 미리 확립한다.
Output: chart-colors.ts (chart3 추가), mock-data.ts (8개 일별 + 4개 주차별 행), period-utils.ts (filterCustomerTypeWeekly), dashboard-content.tsx (확장된 useMemo)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-customer-type-analysis/10-CONTEXT.md
@.planning/phases/10-customer-type-analysis/10-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- 실행자가 수정할 파일들의 현재 상태 — 탐색 불필요 -->

From components/dashboard/charts/chart-colors.ts (현재):
```typescript
export interface ChartColorMode {
  chart1: string;
  chart2: string;
  profitPositive: string;
  profitNegative: string;
  utilizationLine: string;
  referenceOrange: string;
  axis: string;
  grid: string;
  tooltip: { bg: string; border: string };
}

export const CHART_COLORS = {
  light: {
    chart1: 'oklch(0.646 0.222 41.116)',
    chart2: 'oklch(0.6 0.118 184.704)',
    // ...나머지 필드
  } satisfies ChartColorMode,
  dark: {
    chart1: 'oklch(0.488 0.243 264.376)',
    chart2: 'oklch(0.696 0.17 162.48)',
    // ...나머지 필드
  } satisfies ChartColorMode,
} as const;

export function getChartColors(isDark: boolean): ChartColorMode { ... }
```

globals.css에 이미 존재하는 --chart-3/4/5 oklch 값 (추가 불필요, 값만 참조):
- 라이트: --chart-3: oklch(0.398 0.07 227.392), --chart-4: oklch(0.828 0.189 84.429), --chart-5: oklch(0.769 0.188 70.08)
- 다크: --chart-3: oklch(0.769 0.188 70.08), --chart-4: oklch(0.627 0.265 303.9), --chart-5: oklch(0.645 0.246 16.439)

From types/dashboard.ts:
```typescript
export interface CustomerTypeRow {
  date?: string;           // 일별 시트용 (YYYY-MM-DD). 주차별이면 undefined
  week?: string;           // 주차별 시트용. 일별이면 undefined
  roundTripCount: number;  // 왕복_건수
  callCount: number;       // 부름_건수
  oneWayCount: number;     // 편도_건수
}
```

From lib/period-utils.ts (현재 export):
```typescript
export function getDateRange(period: PeriodKey, today?: Date): DateRange
export function filterDailyByPeriod(records: DailyRecord[], range: DateRange): DailyRecord[]
export function parseWeekMonth(week: string): number | null
export function filterWeeklyByPeriod(records: WeeklyRecord[], period: 'this-month' | 'last-month', today?: Date): WeeklyRecord[]
// filterCustomerTypeWeekly 아직 없음 — Task 1에서 추가
```

From components/dashboard/dashboard-content.tsx (현재 filteredData useMemo):
```typescript
const filteredData = useMemo<TeamDashboardData>(() => {
  if (tab === 'daily') {
    const range = getDateRange(period);
    const filtered = filterDailyByPeriod(data.daily, range);
    return { ...data, daily: filtered };         // customerTypeDaily는 미필터링
  } else {
    const weeklyPeriod = (period === 'last-month' ? 'last-month' : 'this-month') as 'this-month' | 'last-month';
    const filtered = filterWeeklyByPeriod(data.weekly, weeklyPeriod);
    return { ...data, weekly: filtered };        // customerTypeWeekly는 미필터링
  }
}, [data, tab, period]);
```

From lib/mock-data.ts (현재):
```typescript
customerTypeDaily: [] as CustomerTypeRow[],     // 빈 배열 — 교체 대상
customerTypeWeekly: [] as CustomerTypeRow[],    // 빈 배열 — 교체 대상
```
기존 일별 mock 날짜 범위: 2026-01-26 ~ 2026-03-08
기존 주차별 mock: "1주차"~"8주차" 형식 (월 없음) — 주차 필터 폴백 동작
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chart-colors.ts에 chart3/4/5 추가 + period-utils.ts에 filterCustomerTypeWeekly 추가</name>
  <files>components/dashboard/charts/chart-colors.ts, lib/period-utils.ts</files>
  <action>
**chart-colors.ts 수정:**

`ChartColorMode` 인터페이스에 `chart3`, `chart4`, `chart5` 세 필드를 `chart2` 바로 아래에 추가한다.
`CHART_COLORS.light`와 `CHART_COLORS.dark` 객체에도 동일 순서로 값을 추가한다. 기존 `satisfies ChartColorMode` 제약이 TypeScript 컴파일 시 타입 정합성을 검증한다.

추가할 값 (globals.css 기준):
- light.chart3: `'oklch(0.398 0.07 227.392)'`   // --chart-3 라이트
- light.chart4: `'oklch(0.828 0.189 84.429)'`   // --chart-4 라이트
- light.chart5: `'oklch(0.769 0.188 70.08)'`    // --chart-5 라이트
- dark.chart3:  `'oklch(0.769 0.188 70.08)'`    // --chart-3 다크
- dark.chart4:  `'oklch(0.627 0.265 303.9)'`    // --chart-4 다크
- dark.chart5:  `'oklch(0.645 0.246 16.439)'`   // --chart-5 다크

주의: globals.css에 --chart-3/4/5가 이미 존재함. CSS 파일은 수정하지 않는다.

---

**period-utils.ts 수정:**

파일 상단 import에 `CustomerTypeRow`를 `@/types/dashboard`에서 추가한다.

`filterWeeklyByPeriod` 함수 아래에 `filterCustomerTypeWeekly` 함수를 추가하고 export한다:

```typescript
/**
 * CustomerTypeRow 배열을 기간(이번 달/지난 달)으로 필터링한다.
 * filterWeeklyByPeriod와 동일한 로직 — week 필드 기준 월 파싱.
 * - 파싱 불가 레코드(week 없음 포함)가 하나라도 있으면: 전체 반환 (폴백)
 */
export function filterCustomerTypeWeekly(
  rows: CustomerTypeRow[],
  period: 'this-month' | 'last-month',
  today: Date = new Date(),
): CustomerTypeRow[] {
  const currentMonth = today.getMonth() + 1;
  const lastMonth = currentMonth === 1 ? 12 : currentMonth - 1;
  const targetMonth = period === 'this-month' ? currentMonth : lastMonth;

  const hasUnparseable = rows.some((r) => !r.week || parseWeekMonth(r.week) === null);
  if (hasUnparseable) return rows;

  return rows.filter((r) => r.week !== undefined && parseWeekMonth(r.week) === targetMonth);
}
```
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript 빌드 통과. ChartColorMode에 chart3/4/5 포함. period-utils.ts에서 filterCustomerTypeWeekly exported.</done>
</task>

<task type="auto">
  <name>Task 2: mock-data.ts 샘플 데이터 채우기</name>
  <files>lib/mock-data.ts</files>
  <action>
`mockTeamDashboardData` 내 `customerTypeDaily`와 `customerTypeWeekly`를 빈 배열에서 실제 샘플 데이터로 교체한다.

파일 상단의 named const 선언 방식으로 분리 후 참조하거나 인라인으로 작성해도 무방하다.

**customerTypeDaily** — 일별 샘플 (기존 mockDailyRecords 날짜 범위와 일치):

이번 달 (2026-03) 행 — this-month 필터에서 노출:
```typescript
{ date: "2026-03-01", roundTripCount: 18, callCount: 12, oneWayCount: 6 },
{ date: "2026-03-02", roundTripCount: 15, callCount: 10, oneWayCount: 5 },
{ date: "2026-03-03", roundTripCount: 22, callCount: 16, oneWayCount: 8 },
{ date: "2026-03-04", roundTripCount: 12, callCount: 8, oneWayCount: 4 },
{ date: "2026-03-05", roundTripCount: 20, callCount: 14, oneWayCount: 7 },
```

지난 달 (2026-02) 행 — last-month 필터에서 노출:
```typescript
{ date: "2026-02-21", roundTripCount: 16, callCount: 11, oneWayCount: 5 },
{ date: "2026-02-22", roundTripCount: 19, callCount: 13, oneWayCount: 6 },
{ date: "2026-02-23", roundTripCount: 14, callCount: 9, oneWayCount: 4 },
```

**customerTypeWeekly** — 주차별 샘플 (parseWeekMonth가 파싱 가능한 "N월 N주차" 형식 사용):
```typescript
{ week: "2월 1주차", roundTripCount: 108, callCount: 72, oneWayCount: 35 },
{ week: "2월 2주차", roundTripCount: 115, callCount: 80, oneWayCount: 45 },
{ week: "3월 1주차", roundTripCount: 98, callCount: 65, oneWayCount: 32 },
{ week: "3월 2주차", roundTripCount: 105, callCount: 70, oneWayCount: 38 },
```

주의: 주차 형식을 "N월 N주차"로 해야 parseWeekMonth가 월을 파싱할 수 있다.
기존 mockWeeklyRecords의 "1주차" 형식(월 없음)은 건드리지 않는다 — 기존 차트 테스트에서 폴백 동작을 검증하는 의도적 형식이다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>빌드 통과. mockTeamDashboardData.customerTypeDaily에 5개 이상 행, customerTypeWeekly에 4개 행 존재. 각 행에 date/week, roundTripCount, callCount, oneWayCount 필드 모두 포함.</done>
</task>

<task type="auto">
  <name>Task 3: dashboard-content.tsx filteredData useMemo 확장 (CTYPE-03)</name>
  <files>components/dashboard/dashboard-content.tsx</files>
  <action>
`dashboard-content.tsx`의 import 라인과 `filteredData` useMemo를 수정한다.

**import 추가:**
기존 `filterWeeklyByPeriod` import 라인에 `filterCustomerTypeWeekly`를 추가한다:
```typescript
import {
  type PeriodKey,
  getDateRange,
  filterDailyByPeriod,
  filterWeeklyByPeriod,
  filterCustomerTypeWeekly,   // 추가
  DEFAULT_DAILY_PERIOD,
  DEFAULT_WEEKLY_PERIOD,
  DAILY_PERIODS,
} from '@/lib/period-utils';
```

**filteredData useMemo 수정:**

Daily 분기에 customerTypeDaily 필터링 추가:
```typescript
if (tab === 'daily') {
  const range = getDateRange(period);
  const filtered = filterDailyByPeriod(data.daily, range);
  // 고객 유형 일별 데이터도 동일한 날짜 범위로 필터링
  const filteredCustomerTypeDaily = data.customerTypeDaily.filter(
    (r) => r.date !== undefined && r.date >= range.start && r.date <= range.end
  );
  return { ...data, daily: filtered, customerTypeDaily: filteredCustomerTypeDaily };
}
```

Weekly 분기에 customerTypeWeekly 필터링 추가:
```typescript
} else {
  const weeklyPeriod = (period === 'last-month' ? 'last-month' : 'this-month') as
    | 'this-month'
    | 'last-month';
  const filtered = filterWeeklyByPeriod(data.weekly, weeklyPeriod);
  // 고객 유형 주차별 데이터도 동일한 월 기준으로 필터링
  const filteredCustomerTypeWeekly = filterCustomerTypeWeekly(data.customerTypeWeekly, weeklyPeriod);
  return { ...data, weekly: filtered, customerTypeWeekly: filteredCustomerTypeWeekly };
}
```

주의: `filteredData`를 받는 `ChartsSection`, `KpiCards`, `DataTable` 컴포넌트는 인터페이스 변경 없음 — `TeamDashboardData` 타입은 동일, 값만 필터링된다.
  </action>
  <verify>
    <automated>cd /c/Users/User/hodo/sheet-dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>빌드 통과. filteredData useMemo가 daily 분기에서 customerTypeDaily를 날짜 범위로 필터링하고, weekly 분기에서 customerTypeWeekly를 월 기준으로 필터링한다. 기존 daily/weekly 필터링 로직은 그대로 유지.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/User/hodo/sheet-dashboard && npm run build
```
빌드가 에러 없이 통과하면 Plan 01 완료.
TypeScript 타입 검사: `ChartColorMode satisfies` 제약이 chart3/4/5 누락 시 컴파일 에러를 발생시켜 자동으로 검증된다.
</verification>

<success_criteria>
- `npm run build` 에러 없음
- `ChartColorMode`에 chart3, chart4, chart5 필드 존재
- `getChartColors(true).chart3` 및 `getChartColors(false).chart3` 값 반환 가능
- `filterCustomerTypeWeekly` period-utils.ts에서 exported
- `mockTeamDashboardData.customerTypeDaily`에 2026-02/03 행 존재 (빈 배열 아님)
- `mockTeamDashboardData.customerTypeWeekly`에 "N월 N주차" 형식 행 4개 존재
- `filteredData.customerTypeDaily`가 기간 필터 적용된 배열로 반환됨 (dashboard-content.tsx)
</success_criteria>

<output>
완료 후 `.planning/phases/10-customer-type-analysis/10-01-SUMMARY.md` 생성
</output>
