---
phase: 01-data-layer-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - types/dashboard.ts
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "DailyRecord 인터페이스가 존재하며 date, revenue, profit, usageHours, usageCount, utilizationRate 필드를 가진다 (monthlyTarget 없음)"
    - "WeeklyRecord 인터페이스가 존재하며 week, revenue, profit, usageHours, usageCount, utilizationRate, weeklyTarget 필드를 가진다"
    - "TeamDashboardData 인터페이스가 daily: DailyRecord[], weekly: WeeklyRecord[], fetchedAt: string 필드를 가진다"
    - "기존 범용 타입 KpiData, MonthlyRevenue, CategoryDistribution, RecentOrder, DashboardData가 파일에서 완전히 삭제되었다"
  artifacts:
    - path: "types/dashboard.ts"
      provides: "팀 전용 TypeScript 타입 정의"
      exports:
        - DailyRecord
        - WeeklyRecord
        - TeamDashboardData
  key_links:
    - from: "types/dashboard.ts"
      to: "lib/mock-data.ts"
      via: "import { DailyRecord, WeeklyRecord, TeamDashboardData }"
      pattern: "import.*DailyRecord.*from.*types/dashboard"
    - from: "types/dashboard.ts"
      to: "lib/data.ts"
      via: "import { DailyRecord, WeeklyRecord, TeamDashboardData }"
      pattern: "import.*TeamDashboardData.*from.*types/dashboard"
---

<objective>
기존 e-커머스 범용 타입을 삭제하고 경남울산사업팀 전용 TypeScript 타입으로 완전 교체한다.

Purpose: 이후 모든 파서, mock 데이터, UI 컴포넌트가 의존하는 타입 계약을 확립한다. types/dashboard.ts 교체가 먼저 이루어져야 mock-data.ts와 data.ts를 안전하게 작성할 수 있다.
Output: DailyRecord, WeeklyRecord, TeamDashboardData 인터페이스를 export하는 types/dashboard.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 팀 전용 TypeScript 타입 정의로 완전 교체</name>
  <files>types/dashboard.ts</files>
  <action>
    types/dashboard.ts 파일을 완전히 재작성한다. 기존 타입(KpiData, MonthlyRevenue, CategoryDistribution, RecentOrder, DashboardData)을 전부 삭제하고 아래 세 인터페이스로 교체한다.

    작성 규칙:
    - 기존 타입을 @deprecated로 표시하거나 병렬 유지하지 말 것 — 한 번에 전부 삭제
    - DailyRecord와 WeeklyRecord는 공유 베이스 타입 없이 완전히 독립된 인터페이스로 정의
    - CONTEXT.md 결정: 목표(target) 데이터는 WeeklyRecord에만 존재. DailyRecord에는 monthlyTarget 또는 target 필드가 없다

    작성할 최종 타입:

    ```typescript
    // 주석은 한국어로 작성 (CLAUDE.md 규칙)

    /** Daily 시트 한 행 — 일자별 기록 */
    export interface DailyRecord {
      date: string;             // 일자 (시트 원본 형식 그대로, 예: "2026-02-21")
      revenue: number;          // 매출 (원)
      profit: number;           // 손익 (원, 음수 가능)
      usageHours: number;       // 이용시간 (시간)
      usageCount: number;       // 이용건수
      utilizationRate: number;  // 가동률 (%, 0-100 범위)
      // monthlyTarget 없음 — Daily 시트에는 목표 컬럼이 존재하지 않음 (CONTEXT.md 결정)
    }

    /** Weekly 시트 한 행 — 주차별 기록 */
    export interface WeeklyRecord {
      week: string;             // 주차 (시트 원본 형식 그대로, 예: "1주차", "2월 3주차")
      revenue: number;          // 매출 (원)
      profit: number;           // 손익 (원, 음수 가능)
      usageHours: number;       // 이용시간 (시간)
      usageCount: number;       // 이용건수
      utilizationRate: number;  // 가동률 (%, 0-100 범위)
      weeklyTarget: number;     // 주차 목표 (원) — Weekly 전용 필드
    }

    /** 대시보드 전체 데이터 컨테이너 */
    export interface TeamDashboardData {
      daily: DailyRecord[];
      weekly: WeeklyRecord[];
      fetchedAt: string;        // ISO 8601 타임스탬프 (예: "2026-02-22T09:00:00.000Z")
    }
    ```

    파일 상단에 한국어 주석으로 파일 목적을 설명한다:
    "// 경남울산사업팀 매출 대시보드 — 팀 전용 타입 정의"

    작성 후 주의사항:
    - app/(dashboard)/dashboard/page.tsx 등 기존 컴포넌트에서 TypeScript 에러가 발생하는 것은 정상이다.
    - 이 에러들은 Phase 2에서 수정하며, Phase 1에서는 수정하지 않는다.
  </action>
  <verify>
    npx tsc --noEmit --project tsconfig.json 2>&1 | grep "types/dashboard.ts" | head -5
    위 명령 실행 시 types/dashboard.ts 자체에서 오류가 없어야 한다.
    (다른 파일에서 import 오류가 나는 것은 허용 — Phase 2에서 수정 예정)

    또한 다음 세 export가 존재하는지 확인:
    grep -E "export interface (DailyRecord|WeeklyRecord|TeamDashboardData)" types/dashboard.ts
    — 3개 모두 출력되어야 한다.

    KpiData, DashboardData 등 구 타입이 삭제되었는지 확인:
    grep -E "KpiData|MonthlyRevenue|CategoryDistribution|RecentOrder|DashboardData" types/dashboard.ts
    — 아무것도 출력되지 않아야 한다.
  </verify>
  <done>
    types/dashboard.ts에 DailyRecord, WeeklyRecord, TeamDashboardData 세 인터페이스가 export되고,
    기존 KpiData, MonthlyRevenue, CategoryDistribution, RecentOrder, DashboardData가 파일에서 완전히 제거되었다.
    types/dashboard.ts 파일 자체에 TypeScript 컴파일 오류가 없다.
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit 2>&1 | grep "types/dashboard.ts"
— types/dashboard.ts 파일 내부에 오류 없음.

grep -c "export interface" types/dashboard.ts
— 출력값이 3 (DailyRecord, WeeklyRecord, TeamDashboardData).

grep "weeklyTarget" types/dashboard.ts
— WeeklyRecord에 weeklyTarget 필드 존재.

grep "monthlyTarget\|DashboardData\|KpiData" types/dashboard.ts
— 아무것도 출력되지 않음.
</verification>

<success_criteria>
- types/dashboard.ts가 DailyRecord, WeeklyRecord, TeamDashboardData 세 인터페이스만 export한다
- DailyRecord에는 monthlyTarget이 없다 (CONTEXT.md 결정)
- WeeklyRecord에는 weeklyTarget이 있다
- 기존 범용 타입 5개(KpiData, MonthlyRevenue, CategoryDistribution, RecentOrder, DashboardData)가 삭제되었다
- 파일 자체에 TypeScript 컴파일 오류가 없다
</success_criteria>

<output>
완료 후 `.planning/phases/01-data-layer-foundation/01-01-SUMMARY.md`를 생성한다.
</output>
