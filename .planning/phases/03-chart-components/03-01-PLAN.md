---
phase: 03-chart-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/charts/chart-colors.ts
  - components/dashboard/charts/charts-skeleton.tsx
  - components/dashboard/charts/charts-section.tsx
autonomous: true
requirements: [CHART-05]

must_haves:
  truths:
    - "chart-colors.ts가 라이트/다크 색상 상수를 export하고 getChartColors(isDark) 함수를 제공한다"
    - "ChartsSkeleton이 4개 차트 영역을 모방하는 Skeleton 플레이스홀더를 렌더링한다"
    - "ChartsSection이 tab prop을 받아 daily/weekly 레코드를 분기하고 4개 차트 자리를 1열로 렌더링한다 (차트 컴포넌트 임시 placeholder)"
  artifacts:
    - path: "components/dashboard/charts/chart-colors.ts"
      provides: "테마별 색상 상수 + getChartColors() 헬퍼"
      exports: ["CHART_COLORS", "getChartColors", "ChartColorMode"]
    - path: "components/dashboard/charts/charts-skeleton.tsx"
      provides: "Suspense fallback 스켈레턴 — 4개 카드 높이 280px"
      exports: ["ChartsSkeleton"]
    - path: "components/dashboard/charts/charts-section.tsx"
      provides: "Server Component 래퍼 — 4개 차트 1열 배치"
      exports: ["ChartsSection"]
  key_links:
    - from: "components/dashboard/charts/charts-section.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "import (ChartsSection는 직접 사용 안 하지만 파일 존재 확인)"
      pattern: "from.*chart-colors"
    - from: "components/dashboard/charts/charts-section.tsx"
      to: "types/dashboard.ts"
      via: "import TeamDashboardData"
      pattern: "import.*TeamDashboardData"
---

<objective>
차트 공유 인프라 구축 — 색상 헬퍼, 스켈레턴, ChartsSection 래퍼를 만들어 03-02/03-03의 개별 차트 구현을 위한 공통 기반을 준비한다.

Purpose: 모든 차트가 동일한 테마 색상 시스템을 참조하고, Suspense fallback과 1열 레이아웃 래퍼를 통해 일관된 UX를 제공한다.
Output: chart-colors.ts, charts-skeleton.tsx, charts-section.tsx (3개 파일)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chart-components/03-CONTEXT.md
@.planning/phases/03-chart-components/03-RESEARCH.md
@types/dashboard.ts
@app/globals.css
@components/dashboard/kpi-cards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: chart-colors.ts — 테마별 색상 상수 및 getChartColors 헬퍼</name>
  <files>components/dashboard/charts/chart-colors.ts</files>
  <action>
`components/dashboard/charts/` 디렉토리를 생성하고 `chart-colors.ts` 파일을 작성한다.

Recharts SVG 속성(fill/stroke)에는 CSS 변수가 직접 동작하지 않으므로(SVG attribute 파싱 제한), 라이트/다크 테마별 하드코딩 색상 값을 상수로 관리한다. 기존 `category-chart.tsx`가 이미 이 패턴을 사용 중이다(HSL 하드코딩).

RESEARCH.md의 Pattern 2를 따라 정확히 구현:

```typescript
// components/dashboard/charts/chart-colors.ts
// Recharts SVG 속성(fill/stroke)에는 CSS 변수가 직접 불가 — 테마별 색상 상수로 관리

export const CHART_COLORS = {
  light: {
    chart1: 'oklch(0.646 0.222 41.116)',   // --chart-1 라이트 (globals.css)
    chart2: 'oklch(0.6 0.118 184.704)',    // --chart-2 라이트
    profitPositive: '#16a34a',             // green-600 (KPI 카드 델타와 동일)
    profitNegative: '#dc2626',             // red-600
    utilizationLine: 'oklch(0.6 0.118 184.704)', // --chart-2
    referenceOrange: '#f97316',            // orange-500 임계선
    axis: '#71717a',                       // zinc-500
    grid: '#e4e4e7',                       // zinc-200
    tooltip: { bg: '#ffffff', border: '#e4e4e7' },
  },
  dark: {
    chart1: 'oklch(0.488 0.243 264.376)', // --chart-1 다크 (globals.css)
    chart2: 'oklch(0.696 0.17 162.48)',   // --chart-2 다크
    profitPositive: '#4ade80',             // green-400
    profitNegative: '#f87171',             // red-400
    utilizationLine: 'oklch(0.696 0.17 162.48)',
    referenceOrange: '#fb923c',            // orange-400
    axis: '#a1a1aa',                       // zinc-400
    grid: '#3f3f46',                       // zinc-700
    tooltip: { bg: '#1c1c1e', border: '#3f3f46' },
  },
} as const;

export type ChartColorMode = typeof CHART_COLORS.light;

export function getChartColors(isDark: boolean): ChartColorMode {
  return isDark ? CHART_COLORS.dark : CHART_COLORS.light;
}
```

주의: oklch literal 값이 SVG fill attribute에서 작동하지 않을 경우(RESEARCH.md Open Question 1) hex 폴백이 필요할 수 있으나, 우선 oklch로 구현하고 브라우저 검증 후 수정한다.
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>components/dashboard/charts/chart-colors.ts 파일 존재 + CHART_COLORS, getChartColors export 확인</manual>
  </verify>
  <done>chart-colors.ts가 타입 에러 없이 컴파일되고 CHART_COLORS, getChartColors, ChartColorMode가 export된다</done>
</task>

<task type="auto">
  <name>Task 2: charts-skeleton.tsx + charts-section.tsx — Suspense fallback 및 Section 래퍼</name>
  <files>
    components/dashboard/charts/charts-skeleton.tsx
    components/dashboard/charts/charts-section.tsx
  </files>
  <action>
**charts-skeleton.tsx**: RESEARCH.md ChartsSkeleton 예시를 따라 4개 Card 스켈레턴을 `space-y-6`으로 배치. 각 Card는 CardHeader에 Skeleton h-5 w-24 (제목 영역), CardContent에 Skeleton h-[280px] w-full (차트 영역).

```typescript
// components/dashboard/charts/charts-skeleton.tsx
import { Card, CardContent, CardHeader } from '@/components/ui/card';
import { Skeleton } from '@/components/ui/skeleton';

export function ChartsSkeleton() {
  return (
    <div className="space-y-6">
      {Array.from({ length: 4 }).map((_, i) => (
        <Card key={i}>
          <CardHeader className="pb-2">
            <Skeleton className="h-5 w-24" />
          </CardHeader>
          <CardContent>
            <Skeleton className="h-[280px] w-full" />
          </CardContent>
        </Card>
      ))}
    </div>
  );
}
```

**charts-section.tsx**: Server Component (no "use client"). `ChartsSection`은 `data: TeamDashboardData`와 `tab: 'daily' | 'weekly'` prop을 받는다.

- Daily: `[...data.daily].sort((a, b) => a.date.localeCompare(b.date)).slice(-30)` — 최근 30일
- Weekly: `data.weekly` 그대로

이 Task 시점에서 개별 차트 컴포넌트(03-02/03-03)가 아직 없으므로, ChartsSkeleton과 동일한 Card 4개를 임시 placeholder로 렌더링한다. 03-04 Plan에서 실제 차트로 교체된다. 이때 import 오류 없이 빌드가 통과해야 한다.

단, 03-02/03-03이 없는 상태이므로 charts-section.tsx에서는 실제 차트 import 없이 다음처럼 작성:

```typescript
// components/dashboard/charts/charts-section.tsx
// Server Component — "use client" 없음
import type { TeamDashboardData } from '@/types/dashboard';
import { ChartsSkeleton } from './charts-skeleton';

interface ChartsSectionProps {
  data: TeamDashboardData;
  tab: 'daily' | 'weekly';
}

// TODO(03-02, 03-03): 개별 차트 컴포넌트 완성 후 교체 예정
export function ChartsSection({ data: _data, tab: _tab }: ChartsSectionProps) {
  // 03-02/03-03 완성 전 임시 placeholder
  return <ChartsSkeleton />;
}
```

타입 에러 없이 빌드 가능해야 한다. `_data`, `_tab`으로 미사용 변수 경고 억제.
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npm run build 2>&1 | tail -20</automated>
    <manual>charts-skeleton.tsx, charts-section.tsx 파일 존재 확인</manual>
  </verify>
  <done>npm run build가 오류 없이 성공하고 ChartsSkeleton, ChartsSection이 올바른 타입으로 export된다</done>
</task>

</tasks>

<verification>
npm run build 성공 + npx tsc --noEmit 타입 에러 없음. 3개 파일이 components/dashboard/charts/ 디렉토리에 존재.
</verification>

<success_criteria>
1. components/dashboard/charts/chart-colors.ts — CHART_COLORS 상수 + getChartColors() export
2. components/dashboard/charts/charts-skeleton.tsx — 4개 Card Skeleton Suspense fallback
3. components/dashboard/charts/charts-section.tsx — Server Component, TeamDashboardData + tab prop, 임시 placeholder
4. npm run build 성공 (타입 오류 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-components/03-01-SUMMARY.md`
</output>
