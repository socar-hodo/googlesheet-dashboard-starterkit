---
phase: 03-chart-components
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - components/dashboard/charts/revenue-trend-chart.tsx
  - components/dashboard/charts/profit-trend-chart.tsx
autonomous: true
requirements: [CHART-01, CHART-02, CHART-05]

must_haves:
  truths:
    - "매출 추이 차트가 Daily에서 Bar만, Weekly에서 Bar + 목표 Line을 ComposedChart로 렌더링한다"
    - "손익 추이 차트가 양수는 green-600/green-400, 음수는 red-600/red-400 색상 Bar로 렌더링한다"
    - "두 차트 모두 useTheme().resolvedTheme으로 라이트/다크 테마 색상을 분기한다"
    - "툴팁이 '실적: ₩6,950만' 형식으로 수치 + 단위를 표시한다"
    - "X축이 Daily에서 M/D 형식(예: 2/1), Weekly에서 '1주' 형식 레이블을 표시한다"
  artifacts:
    - path: "components/dashboard/charts/revenue-trend-chart.tsx"
      provides: "CHART-01 매출 추이 ComposedChart (Bar + optional Line)"
      exports: ["RevenueTrendChart"]
      min_lines: 60
    - path: "components/dashboard/charts/profit-trend-chart.tsx"
      provides: "CHART-02 손익 추이 BarChart + Cell 양수/음수 색상"
      exports: ["ProfitTrendChart"]
      min_lines: 60
  key_links:
    - from: "components/dashboard/charts/revenue-trend-chart.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "getChartColors(resolvedTheme === 'dark')"
      pattern: "getChartColors"
    - from: "components/dashboard/charts/profit-trend-chart.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "getChartColors(resolvedTheme === 'dark')"
      pattern: "getChartColors"
    - from: "components/dashboard/charts/revenue-trend-chart.tsx"
      to: "recharts"
      via: "ComposedChart, Bar, Line import"
      pattern: "from 'recharts'"
---

<objective>
매출 추이(CHART-01)와 손익 추이(CHART-02) 차트 컴포넌트 구현.

Purpose: 핵심 매출/손익 데이터를 시각화하여 기간별 트렌드와 목표 달성 여부를 한눈에 파악할 수 있게 한다.
Output: revenue-trend-chart.tsx (ComposedChart), profit-trend-chart.tsx (BarChart + Cell)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-chart-components/03-CONTEXT.md
@.planning/phases/03-chart-components/03-RESEARCH.md
@.planning/phases/03-chart-components/03-01-SUMMARY.md
@types/dashboard.ts
@components/dashboard/charts/chart-colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: revenue-trend-chart.tsx — 매출 추이 ComposedChart (CHART-01)</name>
  <files>components/dashboard/charts/revenue-trend-chart.tsx</files>
  <action>
`"use client"` 선언. RESEARCH.md Pattern 1, 3, 7을 따라 구현.

**컴포넌트 시그니처:**
```typescript
interface RevenueTrendChartProps {
  records: DailyRecord[] | WeeklyRecord[];
  tab: 'daily' | 'weekly';
}
export function RevenueTrendChart({ records, tab }: RevenueTrendChartProps)
```

**chartData 변환:**
- `label`: Daily → `formatDailyLabel(r.date)` (M/D 형식: `"2026-02-01"` → `"2/1"`), Weekly → `formatWeeklyLabel(r.week)` (`"주차"` → `"주"`)
- `revenue`: `r.revenue`
- `target`: Weekly → `(r as WeeklyRecord).weeklyTarget`, Daily → `undefined`

**Recharts 구조:**
- `<ResponsiveContainer width="100%" height="100%">` (부모 `<div className="h-[280px]">`)
- `<ComposedChart data={chartData} margin={{ top: 5, right: 20, left: 10, bottom: 5 }}>`
- `<CartesianGrid strokeDasharray="3 3" stroke={colors.grid} />`
- `<XAxis dataKey="label" tick={{ fill: colors.axis, fontSize: 11 }} />`
- `<YAxis tick={{ fill: colors.axis, fontSize: 11 }} tickFormatter={(v) => \`\${(v/10000).toLocaleString()}만\`} width={55} />`
- `<Tooltip>`: `formatter={(value, name) => [\`₩\${(Number(value)/10000).toLocaleString()}만\`, name === 'revenue' ? '실적' : '목표']}`, `contentStyle={{ backgroundColor: colors.tooltip.bg, border: \`1px solid \${colors.tooltip.border}\`, borderRadius: '8px', fontSize: '12px' }}`
- `<Bar dataKey="revenue" fill={colors.chart1} name="revenue" radius={[2, 2, 0, 0]} />`
- Weekly 전용 Line: `{tab === 'weekly' && <Line type="monotone" dataKey="target" stroke={colors.chart2} strokeWidth={2} dot={{ fill: colors.chart2, r: 3 }} name="target" />}`

**Card 래퍼:** shadcn Card > CardHeader(`<CardTitle>매출 추이</CardTitle>`) > CardContent > div.h-[280px] > ResponsiveContainer

**테마 처리:**
```typescript
const { resolvedTheme } = useTheme();
const colors = getChartColors(resolvedTheme === 'dark');
```

**금지사항 (RESEARCH.md Anti-Patterns):**
- `fill="var(--chart-1)"` 절대 사용 금지 — SVG에서 무시됨
- `theme` 대신 `resolvedTheme` 사용
- Daily에서 `<Line>` 렌더링 금지 (`tab === 'weekly'` 조건부)
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>revenue-trend-chart.tsx 파일 존재, RevenueTrendChart export 확인</manual>
  </verify>
  <done>타입 오류 없이 컴파일되고, RevenueTrendChart가 records + tab prop을 받아 ComposedChart를 렌더링하는 "use client" 컴포넌트로 구현된다</done>
</task>

<task type="auto">
  <name>Task 2: profit-trend-chart.tsx — 손익 추이 BarChart + Cell (CHART-02)</name>
  <files>components/dashboard/charts/profit-trend-chart.tsx</files>
  <action>
`"use client"` 선언. RESEARCH.md Pattern 4를 따라 구현.

**컴포넌트 시그니처:**
```typescript
interface ProfitTrendChartProps {
  records: DailyRecord[] | WeeklyRecord[];
  tab: 'daily' | 'weekly';
}
export function ProfitTrendChart({ records, tab: _tab }: ProfitTrendChartProps)
```

**chartData 변환:**
- `label`: revenue-trend-chart와 동일한 formatDailyLabel / formatWeeklyLabel 유틸 함수 재선언 또는 별도 utils 파일 없이 로컬 선언
- `profit`: `r.profit` (음수 가능)

**Recharts 구조:**
- `<ResponsiveContainer width="100%" height="100%">` (부모 `<div className="h-[280px]">`)
- `<BarChart data={chartData} margin={{ top: 5, right: 20, left: 10, bottom: 5 }}>`
- `<CartesianGrid strokeDasharray="3 3" stroke={colors.grid} />`
- `<XAxis dataKey="label" tick={{ fill: colors.axis, fontSize: 11 }} />`
- `<YAxis tick={{ fill: colors.axis, fontSize: 11 }} tickFormatter={(v) => \`\${(v/10000).toLocaleString()}만\`} width={55} />`
- `<Tooltip>`: formatter로 `Math.abs(value)/10000`을 `₩{n}만` 포맷, name을 `value >= 0 ? '수익' : '손실'`
- `<Bar dataKey="profit" name="손익" radius={[2, 2, 0, 0]}>` 내부에 Cell 렌더링:
  ```tsx
  {chartData.map((entry, index) => (
    <Cell
      key={`cell-${index}`}
      fill={entry.profit >= 0 ? colors.profitPositive : colors.profitNegative}
    />
  ))}
  ```

**CONTEXT.md 결정 준수:** 양수(+) 녹색 = `colors.profitPositive` (라이트: `#16a34a`, 다크: `#4ade80`), 음수(-) 빨간색 = `colors.profitNegative`. KPI 카드 델타 색상과 동일하게.

**Card 래퍼:** shadcn Card > CardHeader(`<CardTitle>손익 추이</CardTitle>`) > CardContent > div.h-[280px] > ResponsiveContainer

**테마 처리:** `const { resolvedTheme } = useTheme(); const colors = getChartColors(resolvedTheme === 'dark');`

Recharts import: `BarChart, Bar, Cell, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer` from 'recharts'
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npm run build 2>&1 | tail -20</automated>
    <manual>profit-trend-chart.tsx 파일 존재, ProfitTrendChart export 확인</manual>
  </verify>
  <done>npm run build 성공. ProfitTrendChart가 양수/음수 profit에 따라 green/red Cell 색상을 분기하는 BarChart를 렌더링한다</done>
</task>

</tasks>

<verification>
npm run build 성공 + tsc --noEmit 타입 에러 없음. 두 컴포넌트가 getChartColors()와 useTheme()을 통해 테마 색상을 올바르게 분기한다.
</verification>

<success_criteria>
1. revenue-trend-chart.tsx — ComposedChart, Daily=Bar만 / Weekly=Bar+Line, 툴팁 ₩만원 단위
2. profit-trend-chart.tsx — BarChart + Cell, profit 양음수로 green/red 분기
3. 두 컴포넌트 모두 "use client", useTheme().resolvedTheme 사용, getChartColors() 호출
4. npm run build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-components/03-02-SUMMARY.md`
</output>
