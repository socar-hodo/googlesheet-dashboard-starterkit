---
phase: 03-chart-components
plan: "03"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - components/dashboard/charts/utilization-trend-chart.tsx
  - components/dashboard/charts/usage-trend-chart.tsx
autonomous: true
requirements: [CHART-03, CHART-04, CHART-05]

must_haves:
  truths:
    - "가동률 추이 차트가 LineChart로 가동률(%)을 렌더링하고 y=80에 주황 점선 ReferenceLine 임계선을 표시한다"
    - "이용건수/이용시간 차트가 이중 YAxis Bar 차트로 좌측=건, 우측=시간 단위로 렌더링한다"
    - "두 차트 모두 useTheme().resolvedTheme으로 라이트/다크 테마 색상을 분기한다"
  artifacts:
    - path: "components/dashboard/charts/utilization-trend-chart.tsx"
      provides: "CHART-03 가동률 추이 LineChart + ReferenceLine"
      exports: ["UtilizationTrendChart"]
      min_lines: 55
    - path: "components/dashboard/charts/usage-trend-chart.tsx"
      provides: "CHART-04 이용건수/이용시간 이중 YAxis Bar 차트"
      exports: ["UsageTrendChart"]
      min_lines: 65
  key_links:
    - from: "components/dashboard/charts/utilization-trend-chart.tsx"
      to: "recharts"
      via: "LineChart, Line, ReferenceLine import"
      pattern: "ReferenceLine"
    - from: "components/dashboard/charts/usage-trend-chart.tsx"
      to: "recharts"
      via: "ComposedChart, Bar, YAxis (dual) import"
      pattern: "yAxisId"
    - from: "components/dashboard/charts/utilization-trend-chart.tsx"
      to: "components/dashboard/charts/chart-colors.ts"
      via: "getChartColors(resolvedTheme === 'dark')"
      pattern: "getChartColors"
---

<objective>
가동률 추이(CHART-03)와 이용건수/이용시간 추이(CHART-04) 차트 컴포넌트 구현.

Purpose: 가동률 임계선(80%) 기반 경보 시각화와 이중 Y축으로 상이한 단위의 두 지표를 한 차트에서 비교할 수 있게 한다.
Output: utilization-trend-chart.tsx (LineChart + ReferenceLine), usage-trend-chart.tsx (이중 YAxis BarChart)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-chart-components/03-CONTEXT.md
@.planning/phases/03-chart-components/03-RESEARCH.md
@.planning/phases/03-chart-components/03-01-SUMMARY.md
@types/dashboard.ts
@components/dashboard/charts/chart-colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: utilization-trend-chart.tsx — 가동률 라인 차트 + ReferenceLine (CHART-03)</name>
  <files>components/dashboard/charts/utilization-trend-chart.tsx</files>
  <action>
`"use client"` 선언. RESEARCH.md Pattern 5를 따라 구현.

**컴포넌트 시그니처:**
```typescript
interface UtilizationTrendChartProps {
  records: DailyRecord[] | WeeklyRecord[];
  tab: 'daily' | 'weekly';
}
export function UtilizationTrendChart({ records, tab }: UtilizationTrendChartProps)
```

**chartData 변환:**
- `label`: Daily → formatDailyLabel, Weekly → formatWeeklyLabel (revenue-trend-chart와 동일 패턴, 로컬 함수)
- `utilizationRate`: `r.utilizationRate`

**Recharts 구조:**
- `<ResponsiveContainer width="100%" height="100%">` (부모 `<div className="h-[280px]">`)
- `<LineChart data={chartData} margin={{ top: 5, right: 30, left: 10, bottom: 5 }}>`
- `<CartesianGrid strokeDasharray="3 3" stroke={colors.grid} />`
- `<XAxis dataKey="label" tick={{ fill: colors.axis, fontSize: 11 }} />`
- `<YAxis tick={{ fill: colors.axis, fontSize: 11 }} tickFormatter={(v) => \`\${v}%\`} domain={[0, 100]} width={40} />`
- `<Tooltip formatter={(value) => [\`\${Number(value).toFixed(1)}%\`, '가동률']} contentStyle={{ ... }}>`
- **ReferenceLine (핵심 CHART-03 요구사항):**
  ```tsx
  <ReferenceLine
    y={80}
    stroke={colors.referenceOrange}
    strokeDasharray="4 4"
    label={{ value: '기준 80%', position: 'insideTopRight', fill: colors.referenceOrange, fontSize: 11 }}
  />
  ```
  주의: `strokeDasharray`는 ReferenceLine에서 SVG passthrough로 정식 지원됨.
- `<Line type="monotone" dataKey="utilizationRate" stroke={colors.chart1} strokeWidth={2} dot={false} activeDot={{ r: 5, fill: colors.chart1 }} name="가동률" />`

**Card 래퍼:** shadcn Card > CardHeader(`<CardTitle>가동률 추이</CardTitle>`) > CardContent > div.h-[280px] > ResponsiveContainer

Recharts import: `LineChart, Line, ReferenceLine, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer` from 'recharts'
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>utilization-trend-chart.tsx 파일 존재, ReferenceLine y={80} 코드 포함 확인</manual>
  </verify>
  <done>타입 오류 없이 컴파일되고, UtilizationTrendChart가 LineChart + y=80 주황 점선 ReferenceLine을 포함하는 "use client" 컴포넌트로 구현된다</done>
</task>

<task type="auto">
  <name>Task 2: usage-trend-chart.tsx — 이용건수/이용시간 이중 YAxis 차트 (CHART-04)</name>
  <files>components/dashboard/charts/usage-trend-chart.tsx</files>
  <action>
`"use client"` 선언. RESEARCH.md Pattern 6을 따라 구현. 이중 YAxis 구현 시 yAxisId 불일치 주의 (Pitfall 3).

**컴포넌트 시그니처:**
```typescript
interface UsageTrendChartProps {
  records: DailyRecord[] | WeeklyRecord[];
  tab: 'daily' | 'weekly';
}
export function UsageTrendChart({ records, tab }: UsageTrendChartProps)
```

**chartData 변환:**
- `label`: Daily → formatDailyLabel, Weekly → formatWeeklyLabel (로컬 함수)
- `usageCount`: `r.usageCount`
- `usageHours`: `r.usageHours`

**Recharts 구조:**
- `<ResponsiveContainer width="100%" height="100%">` (부모 `<div className="h-[280px]">`)
- `<ComposedChart data={chartData} margin={{ top: 5, right: 40, left: 10, bottom: 5 }}>`
- `<CartesianGrid strokeDasharray="3 3" stroke={colors.grid} />`
- `<XAxis dataKey="label" tick={{ fill: colors.axis, fontSize: 11 }} />`
- **좌 Y축 — 이용건수:**
  ```tsx
  <YAxis
    yAxisId="left"
    orientation="left"
    tick={{ fill: colors.axis, fontSize: 11 }}
    tickFormatter={(v) => `${v}건`}
    width={45}
  />
  ```
- **우 Y축 — 이용시간:**
  ```tsx
  <YAxis
    yAxisId="right"
    orientation="right"
    tick={{ fill: colors.axis, fontSize: 11 }}
    tickFormatter={(v) => `${v}h`}
    width={40}
  />
  ```
- `<Tooltip>`: 두 시리즈 모두 표시. `formatter={(value, name) => [name === '이용건수' ? \`\${value}건\` : \`\${value}h\`, name]}`
- **Bar (yAxisId 필수):**
  ```tsx
  <Bar yAxisId="left" dataKey="usageCount" fill={colors.chart1} name="이용건수" radius={[2, 2, 0, 0]} />
  <Bar yAxisId="right" dataKey="usageHours" fill={colors.chart2} name="이용시간" radius={[2, 2, 0, 0]} />
  ```
  반드시 `yAxisId` 값이 YAxis의 `yAxisId` prop과 정확히 일치해야 한다 ("left"/"right").
- `<Legend />` 추가 (이용건수/이용시간 범례 표시)

**Card 래퍼:** shadcn Card > CardHeader(`<CardTitle>이용건수 / 이용시간</CardTitle>`) > CardContent > div.h-[280px] > ResponsiveContainer

Recharts import: `ComposedChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer` from 'recharts'
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npm run build 2>&1 | tail -20</automated>
    <manual>usage-trend-chart.tsx 파일 존재, yAxisId="left"/"right" 패턴 코드 포함 확인</manual>
  </verify>
  <done>npm run build 성공. UsageTrendChart가 이중 YAxis(좌=건, 우=h)와 두 개의 Bar를 렌더링하고, 콘솔에 "Could not find yAxis" 경고가 없다</done>
</task>

</tasks>

<verification>
npm run build 성공 + tsc --noEmit 타입 에러 없음. 두 컴포넌트가 chart-colors.ts와 useTheme()을 사용하여 테마 색상을 올바르게 분기한다.
</verification>

<success_criteria>
1. utilization-trend-chart.tsx — LineChart, ReferenceLine y=80 주황 점선, 가동률% 툴팁
2. usage-trend-chart.tsx — ComposedChart, YAxis×2 (yAxisId="left"/"right"), Bar×2 각각 yAxisId 지정
3. 두 컴포넌트 모두 "use client", useTheme().resolvedTheme 사용
4. npm run build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-components/03-03-SUMMARY.md`
</output>
