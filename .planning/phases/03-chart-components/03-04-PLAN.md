---
phase: 03-chart-components
plan: "04"
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - components/dashboard/charts/charts-section.tsx
  - app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements: [CHART-01, CHART-02, CHART-03, CHART-04, CHART-05]

must_haves:
  truths:
    - "대시보드 페이지에서 KPI 카드 아래에 4개 차트가 1열로 표시된다"
    - "탭 전환 시 ChartsSkeleton이 Suspense fallback으로 표시된다"
    - "Daily 탭에서 4개 차트가 최근 30일 데이터로 렌더링된다"
    - "Weekly 탭에서 4개 차트가 전체 주차 데이터로 렌더링되고 매출 차트에 목표 Line이 표시된다"
    - "라이트/다크 테마 전환 시 모든 차트 색상이 올바르게 변경된다"
  artifacts:
    - path: "components/dashboard/charts/charts-section.tsx"
      provides: "4개 실제 차트 컴포넌트를 조합한 Server Component 래퍼 (최종 버전)"
      exports: ["ChartsSection"]
    - path: "app/(dashboard)/dashboard/page.tsx"
      provides: "ChartsSection + ChartsSkeleton을 Suspense로 감싸 KPI 카드 아래 배치"
      contains: "ChartsSection"
  key_links:
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/charts/charts-section.tsx"
      via: "Suspense key={`charts-${activeTab}`} fallback={<ChartsSkeleton />}"
      pattern: "ChartsSection"
    - from: "components/dashboard/charts/charts-section.tsx"
      to: "components/dashboard/charts/revenue-trend-chart.tsx"
      via: "import RevenueTrendChart"
      pattern: "RevenueTrendChart"
    - from: "components/dashboard/charts/charts-section.tsx"
      to: "types/dashboard.ts"
      via: "TeamDashboardData prop"
      pattern: "TeamDashboardData"
---

<objective>
ChartsSection을 실제 차트 컴포넌트로 교체하고 page.tsx에 연결하여 Phase 3 차트를 대시보드에 통합한다. 브라우저 검증으로 다크/라이트 테마 색상(CHART-05)을 최종 확인한다.

Purpose: 03-01~03-03에서 만든 모든 차트 컴포넌트를 대시보드에 실제로 노출한다. Phase 3의 최종 wiring 단계.
Output: 최종 charts-section.tsx (실제 차트), 업데이트된 page.tsx, 브라우저 검증 통과
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-chart-components/03-CONTEXT.md
@.planning/phases/03-chart-components/03-RESEARCH.md
@.planning/phases/03-chart-components/03-01-SUMMARY.md
@.planning/phases/03-chart-components/03-02-SUMMARY.md
@.planning/phases/03-chart-components/03-03-SUMMARY.md
@app/(dashboard)/dashboard/page.tsx
@types/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: charts-section.tsx 최종 구현 — 4개 차트 컴포넌트 연결</name>
  <files>components/dashboard/charts/charts-section.tsx</files>
  <action>
03-01에서 임시 placeholder(`<ChartsSkeleton />`)로 만든 `charts-section.tsx`를 실제 4개 차트 컴포넌트를 조합하는 최종 버전으로 교체한다.

RESEARCH.md Pattern 1을 따라 구현:

```typescript
// components/dashboard/charts/charts-section.tsx
// Server Component — "use client" 없음
import type { TeamDashboardData, DailyRecord, WeeklyRecord } from '@/types/dashboard';
import { RevenueTrendChart } from './revenue-trend-chart';
import { ProfitTrendChart } from './profit-trend-chart';
import { UtilizationTrendChart } from './utilization-trend-chart';
import { UsageTrendChart } from './usage-trend-chart';

interface ChartsSectionProps {
  data: TeamDashboardData;
  tab: 'daily' | 'weekly';
}

export function ChartsSection({ data, tab }: ChartsSectionProps) {
  // Daily: 날짜 오름차순 정렬 후 최근 30일 슬라이싱 (CONTEXT.md 결정)
  // Weekly: 전체 데이터 그대로
  const records: DailyRecord[] | WeeklyRecord[] = tab === 'daily'
    ? [...data.daily].sort((a, b) => a.date.localeCompare(b.date)).slice(-30)
    : data.weekly;

  return (
    <div className="space-y-6">
      <RevenueTrendChart records={records} tab={tab} />
      <ProfitTrendChart records={records} tab={tab} />
      <UtilizationTrendChart records={records} tab={tab} />
      <UsageTrendChart records={records} tab={tab} />
    </div>
  );
}
```

**핵심:** ChartsSection 자체는 Server Component (no "use client"). 데이터 슬라이싱 로직을 여기서 처리하여 개별 차트에 중복 코드 없음 (RESEARCH.md Open Question 2 결론 적용).

타입 정확성: `records` 변수를 `DailyRecord[] | WeeklyRecord[]`로 명시하여 개별 차트의 union prop과 일치시킨다.
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>charts-section.tsx에 RevenueTrendChart, ProfitTrendChart, UtilizationTrendChart, UsageTrendChart 4개 import 확인</manual>
  </verify>
  <done>타입 오류 없이 컴파일되고, ChartsSection이 4개 차트 컴포넌트를 space-y-6으로 배치하는 Server Component로 완성된다</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx 업데이트 — ChartsSection + ChartsSkeleton Suspense 통합</name>
  <files>app/(dashboard)/dashboard/page.tsx</files>
  <action>
기존 `app/(dashboard)/dashboard/page.tsx`에 ChartsSection과 ChartsSkeleton import를 추가하고, KPI Suspense 블록 아래에 Charts Suspense 블록을 추가한다.

기존 page.tsx 구조 유지하면서 추가:

```typescript
import { ChartsSection } from '@/components/dashboard/charts/charts-section';
import { ChartsSkeleton } from '@/components/dashboard/charts/charts-skeleton';
```

return 내부 `<div className="space-y-6">` 안, KPI Suspense 블록 바로 아래에 추가:

```tsx
{/* CHART-01~05: 차트 섹션 — 탭 전환 시 key 변경으로 스켈레턴 재표시 (RESEARCH.md Pattern 8) */}
<Suspense key={`charts-${activeTab}`} fallback={<ChartsSkeleton />}>
  <ChartsSection data={data} tab={activeTab} />
</Suspense>
```

**Suspense key 규칙:** KPI Suspense key(`activeTab`)와 Charts Suspense key(`charts-${activeTab}`)를 다르게 설정하여 독립적으로 동작하게 한다.

최종 page.tsx 전체 구조:
```tsx
<div className="space-y-6">
  <Suspense fallback={null}>
    <TabNav activeTab={activeTab} />
  </Suspense>

  <Suspense key={activeTab} fallback={<KpiCardsSkeleton />}>
    <KpiCards data={data} tab={activeTab} />
  </Suspense>

  <Suspense key={`charts-${activeTab}`} fallback={<ChartsSkeleton />}>
    <ChartsSection data={data} tab={activeTab} />
  </Suspense>
</div>
```

기존 import/export/`dynamic` 설정은 그대로 유지한다.
  </action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npm run build 2>&1 | tail -20</automated>
    <manual>page.tsx에 ChartsSection import + Suspense key={`charts-${activeTab}`} 블록 존재 확인</manual>
  </verify>
  <done>npm run build 성공. page.tsx가 KPI 카드 아래 Charts Suspense 블록을 포함하고 빌드 오류 없음</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 브라우저 검증 — 차트 렌더링 및 다크/라이트 테마 색상 (CHART-05)</name>
  <what-built>
    Phase 3 차트 4종 완성 — 매출 추이(CHART-01), 손익 추이(CHART-02), 가동률 추이(CHART-03), 이용건수/이용시간(CHART-04)가 대시보드 KPI 카드 아래 1열로 배치됨. CHART-05 테마 색상 브라우저 검증 필요.
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000/dashboard 접속
    2. Daily 탭 확인:
       - KPI 카드 아래 4개 차트가 세로로 나열되는지 확인
       - 매출 추이: Bar만 표시 (Line 없음)
       - 손익 추이: 양수 녹색 Bar, 음수 빨간색 Bar
       - 가동률 추이: 라인 차트 + 주황 점선 기준선(80%)
       - 이용건수/이용시간: 이중 Bar, 좌축=건, 우축=h
    3. Weekly 탭 확인:
       - 매출 추이 차트에 목표 Line이 Bar 위에 오버레이되는지 확인
    4. 다크 모드 전환 후 확인:
       - 모든 차트 색상이 다크 테마(어두운 배경에 적절한 색상)로 변경되는지 확인
       - 차트 색상이 검은색/흰색으로 깨지지 않는지 확인 (oklch SVG 렌더링 이슈 검증)
    5. 각 차트 Bar/Point에 마우스 올렸을 때 툴팁이 올바른 형식으로 표시되는지 확인:
       - 매출: '실적: ₩{n}만' 형식
       - 손익: '수익: ₩{n}만' 또는 '손실: ₩{n}만'
       - 가동률: '{n}%'
  </how-to-verify>
  <resume-signal>
    모든 항목 통과 시 "approved" 입력.
    색상 문제 발생 시 (예: 차트가 검은색으로 표시) 상세 내용 기술.
    oklch 색상이 SVG에서 작동하지 않을 경우 hex 폴백 적용 필요 (chart-colors.ts 수정).
  </resume-signal>
  <action>npm run dev 실행 후 브라우저에서 http://localhost:3000/dashboard 접속하여 위 how-to-verify 항목을 순서대로 확인한다.</action>
  <verify>
    <automated>cd "C:/Users/User/hodo/sheet-dashboard" && npm run build 2>&1 | tail -5</automated>
    <manual>브라우저에서 Daily/Weekly 탭 4개 차트 렌더링, 다크모드 색상 정상 확인</manual>
  </verify>
  <done>4개 차트가 라이트/다크 양 테마에서 올바른 색상으로 렌더링되고 Daily/Weekly 탭 전환 시 올바른 데이터로 표시된다</done>
</task>

</tasks>

<verification>
npm run build 성공 + 브라우저에서 4개 차트가 라이트/다크 양 테마에서 올바른 색상으로 렌더링됨. Daily/Weekly 탭 전환 시 ChartsSkeleton 표시 후 차트 렌더링.
</verification>

<success_criteria>
1. charts-section.tsx — 4개 실제 차트 import, Daily 최근 30일 슬라이싱, Weekly 전체
2. page.tsx — Charts Suspense 블록 추가, KPI 아래 1열 배치
3. npm run build 성공
4. 브라우저 검증 통과: Daily/Weekly 4개 차트 정상 렌더링, 다크모드 색상 정상
</success_criteria>

<output>
After completion, create `.planning/phases/03-chart-components/03-04-SUMMARY.md`
</output>
